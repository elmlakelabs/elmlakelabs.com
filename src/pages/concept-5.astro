---
/**
 * CONCEPT 5: "MINIMAL LUXE"
 * Aesthetic: Premium, refined, editorial
 * Feel: Sophisticated, clean, high-end - like a luxury tech brand
 * Typography: Elegant serif (Playfair) with refined sans (DM Sans)
 * Colors: White/light backgrounds with logo gradient as accent, dark sections
 * Unique element: Generous whitespace, editorial layouts, subtle gradient accents
 */
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Elm Lake Labs | Concept 5 - Minimal Luxe</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&family=DM+Sans:ital,wght@0,400;0,500;0,600;1,400&display=swap" rel="stylesheet">
    <style>
      :root {
        /* Logo-derived colors */
        --magenta: #E91E8C;
        --purple: #9333EA;
        --blue: #0EA5E9;
        --cyan: #06B6D4;

        /* Neutral palette */
        --white: #FFFFFF;
        --off-white: #FAFAFA;
        --cream: #F5F3F0;
        --gray-100: #E5E5E5;
        --gray-300: #B3B3B3;
        --gray-500: #737373;
        --gray-700: #404040;
        --gray-900: #171717;
        --black: #0A0A0A;

        --font-display: 'Playfair Display', Georgia, serif;
        --font-body: 'DM Sans', -apple-system, sans-serif;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }

      html { scroll-behavior: smooth; }

      body {
        font-family: var(--font-body);
        background: var(--white);
        color: var(--gray-900);
        line-height: 1.6;
        overflow-x: hidden;
      }

      /* Concept switcher */
      .concept-nav {
        position: fixed;
        top: 24px;
        right: 24px;
        z-index: 1000;
        display: flex;
        gap: 4px;
        padding: 6px;
        background: var(--gray-900);
      }
      .concept-nav a {
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: var(--font-body);
        font-size: 12px;
        font-weight: 500;
        text-decoration: none;
        color: var(--gray-500);
        background: transparent;
        transition: all 0.3s;
      }
      .concept-nav a:hover {
        color: var(--white);
      }
      .concept-nav a.active {
        background: linear-gradient(135deg, var(--magenta), var(--purple));
        color: var(--white);
      }
      .concept-nav a.home {
        font-size: 14px;
      }

      /* Header */
      header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        padding: 24px 60px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid var(--gray-100);
      }

      .logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .logo img {
        height: 40px;
        width: auto;
      }

      nav {
        display: flex;
        gap: 48px;
      }

      nav a {
        font-size: 13px;
        font-weight: 500;
        color: var(--gray-500);
        text-decoration: none;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        transition: color 0.3s;
        position: relative;
      }

      nav a::after {
        content: '';
        position: absolute;
        bottom: -4px;
        left: 0;
        width: 0;
        height: 2px;
        background: linear-gradient(90deg, var(--magenta), var(--purple));
        transition: width 0.3s;
      }

      nav a:hover {
        color: var(--gray-900);
      }

      nav a:hover::after {
        width: 100%;
      }

      .header-cta {
        padding: 14px 32px;
        background: var(--gray-900);
        color: var(--white);
        font-size: 12px;
        font-weight: 500;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        text-decoration: none;
        transition: all 0.3s;
      }

      .header-cta:hover {
        background: linear-gradient(135deg, var(--magenta), var(--purple));
      }

      /* Hero */
      .hero {
        min-height: 100vh;
        display: grid;
        grid-template-columns: 1fr 1fr;
        align-items: stretch;
      }

      .hero-content {
        padding: 180px 80px 80px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        background: var(--white);
      }

      .hero-eyebrow {
        font-size: 12px;
        font-weight: 500;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        background: linear-gradient(90deg, var(--magenta), var(--purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 24px;
      }

      .hero-title {
        font-family: var(--font-display);
        font-size: clamp(48px, 5vw, 72px);
        font-weight: 400;
        line-height: 1.15;
        margin-bottom: 32px;
        letter-spacing: -0.02em;
      }

      .hero-title em {
        font-style: italic;
        background: linear-gradient(135deg, var(--magenta), var(--purple), var(--blue));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .hero-desc {
        max-width: 400px;
        font-size: 16px;
        color: var(--gray-500);
        line-height: 1.9;
        margin-bottom: 48px;
      }

      .hero-cta-group {
        display: flex;
        gap: 24px;
        align-items: center;
      }

      .btn-primary {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        padding: 18px 40px;
        background: linear-gradient(135deg, var(--magenta), var(--purple));
        color: var(--white);
        font-size: 13px;
        font-weight: 500;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        text-decoration: none;
        transition: all 0.4s;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 40px rgba(147, 51, 234, 0.3);
      }

      .btn-text {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        font-size: 13px;
        font-weight: 500;
        color: var(--gray-900);
        text-decoration: none;
        letter-spacing: 0.05em;
        transition: color 0.3s;
      }

      .btn-text:hover {
        color: var(--purple);
      }

      .hero-visual {
        position: relative;
        overflow: hidden;
      }

      .hero-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        filter: brightness(0.95);
      }

      .hero-caption {
        position: absolute;
        bottom: 40px;
        right: 40px;
        font-family: var(--font-display);
        font-size: 14px;
        font-style: italic;
        color: var(--white);
        background: var(--gray-900);
        padding: 12px 24px;
      }

      /* Stats strip */
      .stats-strip {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        border-top: 1px solid var(--gray-100);
        border-bottom: 1px solid var(--gray-100);
        position: relative;
      }

      .stats-strip::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        height: 2px;
        background: linear-gradient(90deg, var(--magenta), var(--purple), var(--blue), var(--cyan));
      }

      .stat {
        padding: 48px 40px;
        text-align: center;
        border-right: 1px solid var(--gray-100);
      }

      .stat:last-child {
        border-right: none;
      }

      .stat-number {
        font-family: var(--font-display);
        font-size: 48px;
        font-weight: 400;
        color: var(--gray-900);
        margin-bottom: 8px;
      }

      .stat-label {
        font-size: 12px;
        font-weight: 500;
        color: var(--gray-500);
        letter-spacing: 0.15em;
        text-transform: uppercase;
      }

      /* Editorial section */
      .editorial {
        padding: 120px 80px;
      }

      .editorial-header {
        display: grid;
        grid-template-columns: 200px 1fr;
        gap: 80px;
        margin-bottom: 80px;
        align-items: end;
      }

      .section-number {
        font-family: var(--font-display);
        font-size: 120px;
        font-weight: 400;
        background: linear-gradient(135deg, var(--gray-100), var(--cream));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 0.8;
      }

      .section-intro h2 {
        font-family: var(--font-display);
        font-size: clamp(36px, 4vw, 52px);
        font-weight: 400;
        line-height: 1.2;
        margin-bottom: 24px;
        max-width: 600px;
      }

      .section-intro p {
        font-size: 16px;
        color: var(--gray-500);
        line-height: 1.9;
        max-width: 500px;
      }

      .solutions-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 40px;
      }

      .solution-card {
        padding: 40px 32px;
        border: 1px solid var(--gray-100);
        transition: all 0.4s ease;
        position: relative;
      }

      .solution-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 0;
        height: 3px;
        background: linear-gradient(90deg, var(--magenta), var(--purple), var(--blue));
        transition: width 0.4s;
      }

      .solution-card:hover::before {
        width: 100%;
      }

      .solution-card:hover {
        border-color: var(--gray-900);
        transform: translateY(-8px);
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.06);
      }

      .solution-number {
        font-family: var(--font-display);
        font-size: 48px;
        font-weight: 400;
        background: linear-gradient(135deg, var(--gray-100), var(--cream));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 24px;
        line-height: 1;
      }

      .solution-title {
        font-family: var(--font-display);
        font-size: 22px;
        font-weight: 500;
        margin-bottom: 16px;
      }

      .solution-desc {
        font-size: 14px;
        color: var(--gray-500);
        line-height: 1.8;
        margin-bottom: 24px;
      }

      .solution-link {
        font-size: 12px;
        font-weight: 500;
        background: linear-gradient(90deg, var(--magenta), var(--purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-decoration: none;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        transition: opacity 0.3s;
      }

      .solution-link:hover {
        opacity: 0.7;
      }

      /* Products */
      .products {
        padding: 120px 80px;
        background: var(--off-white);
      }

      .products-header {
        text-align: center;
        margin-bottom: 80px;
      }

      .products-header h2 {
        font-family: var(--font-display);
        font-size: clamp(36px, 4vw, 52px);
        font-weight: 400;
        margin-bottom: 16px;
      }

      .products-header p {
        font-size: 16px;
        color: var(--gray-500);
      }

      .products-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 40px;
      }

      .product-card {
        background: var(--white);
        transition: all 0.4s ease;
      }

      .product-card:hover {
        transform: translateY(-12px);
        box-shadow: 0 40px 80px rgba(0, 0, 0, 0.08);
      }

      .product-image-wrapper {
        background: linear-gradient(135deg, rgba(233, 30, 140, 0.03), rgba(147, 51, 234, 0.05), rgba(14, 165, 233, 0.03));
        padding: 48px;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 280px;
        position: relative;
        overflow: hidden;
      }

      .product-image {
        width: 80%;
        height: 180px;
        object-fit: contain;
        transition: transform 0.4s;
      }

      .product-card:hover .product-image {
        transform: scale(1.08);
      }

      .product-content {
        padding: 32px;
      }

      .product-category {
        font-size: 11px;
        font-weight: 500;
        background: linear-gradient(90deg, var(--magenta), var(--purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        margin-bottom: 8px;
      }

      .product-name {
        font-family: var(--font-display);
        font-size: 24px;
        font-weight: 500;
        margin-bottom: 12px;
      }

      .product-desc {
        font-size: 14px;
        color: var(--gray-500);
        line-height: 1.7;
        margin-bottom: 24px;
      }

      .product-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 24px;
        border-top: 1px solid var(--gray-100);
      }

      .product-price {
        font-family: var(--font-display);
        font-size: 28px;
        font-weight: 500;
        color: var(--gray-900);
      }

      .product-link {
        font-size: 12px;
        font-weight: 500;
        color: var(--gray-900);
        text-decoration: none;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: color 0.3s;
      }

      .product-link:hover {
        color: var(--purple);
      }

      /* About */
      .about {
        padding: 120px 80px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 100px;
        align-items: center;
      }

      .about-image-container {
        position: relative;
      }

      .about-image {
        width: 100%;
        height: 600px;
        object-fit: cover;
      }

      .about-image-caption {
        position: absolute;
        bottom: -40px;
        right: -40px;
        background: linear-gradient(135deg, var(--magenta), var(--purple));
        color: var(--white);
        padding: 32px 40px;
        max-width: 280px;
      }

      .about-image-caption span {
        font-family: var(--font-display);
        font-size: 36px;
        display: block;
        margin-bottom: 4px;
      }

      .about-image-caption p {
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        opacity: 0.9;
      }

      .about-content .section-number {
        margin-bottom: 24px;
      }

      .about-content h2 {
        font-family: var(--font-display);
        font-size: clamp(36px, 4vw, 48px);
        font-weight: 400;
        line-height: 1.2;
        margin-bottom: 32px;
      }

      .about-content h2 em {
        font-style: italic;
        background: linear-gradient(135deg, var(--magenta), var(--purple), var(--blue));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .about-content > p {
        font-size: 16px;
        color: var(--gray-500);
        line-height: 1.9;
        margin-bottom: 48px;
      }

      .values-list {
        display: flex;
        flex-direction: column;
        gap: 24px;
      }

      .value-item {
        display: flex;
        align-items: flex-start;
        gap: 24px;
        padding: 24px 0;
        border-bottom: 1px solid var(--gray-100);
      }

      .value-number {
        font-family: var(--font-display);
        font-size: 14px;
        font-style: italic;
        background: linear-gradient(90deg, var(--magenta), var(--purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        min-width: 32px;
      }

      .value-content h4 {
        font-family: var(--font-display);
        font-size: 18px;
        font-weight: 500;
        margin-bottom: 4px;
      }

      .value-content p {
        font-size: 14px;
        color: var(--gray-500);
      }

      /* Contact */
      .contact {
        padding: 160px 80px;
        background: var(--gray-900);
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .contact-glow {
        position: absolute;
        width: 600px;
        height: 600px;
        border-radius: 50%;
        filter: blur(150px);
        opacity: 0.15;
      }

      .contact-glow.left {
        top: -200px;
        left: -200px;
        background: var(--magenta);
      }

      .contact-glow.right {
        bottom: -200px;
        right: -200px;
        background: var(--blue);
      }

      .contact-inner {
        max-width: 700px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
      }

      .contact h2 {
        font-family: var(--font-display);
        font-size: clamp(40px, 5vw, 64px);
        font-weight: 400;
        color: var(--white);
        line-height: 1.2;
        margin-bottom: 24px;
      }

      .contact h2 em {
        font-style: italic;
        background: linear-gradient(135deg, var(--magenta), var(--purple), var(--blue));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .contact p {
        font-size: 16px;
        color: var(--gray-500);
        line-height: 1.8;
        margin-bottom: 48px;
      }

      .contact .btn-primary {
        background: linear-gradient(135deg, var(--magenta), var(--purple));
      }

      .contact .btn-primary:hover {
        box-shadow: 0 20px 60px rgba(147, 51, 234, 0.4);
      }

      /* Footer */
      footer {
        background: var(--gray-900);
        padding: 60px 80px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
      }

      .footer-logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .footer-logo img {
        height: 32px;
        width: auto;
      }

      .footer-links {
        display: flex;
        gap: 40px;
      }

      .footer-links a {
        font-size: 12px;
        font-weight: 500;
        color: var(--gray-500);
        text-decoration: none;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        transition: color 0.3s;
      }

      .footer-links a:hover {
        color: var(--white);
      }

      .footer-copy {
        font-size: 12px;
        color: var(--gray-500);
      }

      /* Responsive */
      @media (max-width: 1200px) {
        .solutions-grid { grid-template-columns: repeat(2, 1fr); }
      }

      @media (max-width: 1024px) {
        .hero { grid-template-columns: 1fr; }
        .hero-content { padding: 160px 40px 80px; }
        .hero-visual { height: 50vh; }
        .stats-strip { grid-template-columns: repeat(2, 1fr); }
        .editorial { padding: 80px 40px; }
        .editorial-header { grid-template-columns: 1fr; gap: 24px; }
        .section-number { font-size: 80px; }
        .solutions-grid { grid-template-columns: 1fr; }
        .products { padding: 80px 40px; }
        .products-grid { grid-template-columns: 1fr; }
        .about { grid-template-columns: 1fr; padding: 80px 40px; gap: 60px; }
        .about-image-container { order: -1; }
        .about-image { height: 400px; }
        .about-image-caption { right: 20px; bottom: -30px; }
        header { padding: 16px 24px; }
        nav { display: none; }
        .contact { padding: 80px 40px; }
        footer { flex-direction: column; gap: 32px; text-align: center; padding: 48px 40px; }
        .footer-links { flex-wrap: wrap; justify-content: center; }
      }
    </style>
  </head>
  <body>
    <!-- Concept Switcher -->
    <div class="concept-nav">
      <a href="/" class="home" title="Original">&#8962;</a>
      <a href="/concept-1" title="Soft Gradient">1</a>
      <a href="/concept-2" title="Dark Tech">2</a>
      <a href="/concept-3" title="Aurora Flow">3</a>
      <a href="/concept-4" title="Gradient Industrial">4</a>
      <a href="/concept-5" class="active" title="Minimal Luxe">5</a>
    </div>

    <header>
      <a href="/" class="logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <nav>
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
      <a href="https://store.elmlakelabs.com" class="header-cta" target="_blank">Shop Now</a>
    </header>

    <section class="hero">
      <div class="hero-content">
        <span class="hero-eyebrow">Precision Agriculture</span>
        <h1 class="hero-title">
          The Art of<br />
          <em>Precision Farming</em>
        </h1>
        <p class="hero-desc">
          State-of-the-art GPS guidance and auto-steer systems that bring
          centimeter-level accuracy to every pass across your fields.
        </p>
        <div class="hero-cta-group">
          <a href="#products" class="btn-primary">
            Explore Collection
          </a>
          <a href="#about" class="btn-text">
            Our Philosophy
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
        </div>
      </div>
      <div class="hero-visual">
        <img src="/images/hero-field.jpg" alt="Agricultural landscape" class="hero-image" />
        <div class="hero-caption">Wisconsin, 2024</div>
      </div>
    </section>

    <div class="stats-strip">
      <div class="stat">
        <div class="stat-number">100+</div>
        <div class="stat-label">Farms Equipped</div>
      </div>
      <div class="stat">
        <div class="stat-number">&#177;1"</div>
        <div class="stat-label">RTK Accuracy</div>
      </div>
      <div class="stat">
        <div class="stat-number">24/7</div>
        <div class="stat-label">Expert Support</div>
      </div>
      <div class="stat">
        <div class="stat-number">15%</div>
        <div class="stat-label">Fuel Savings</div>
      </div>
    </div>

    <section id="solutions" class="editorial">
      <div class="editorial-header">
        <div class="section-number">01</div>
        <div class="section-intro">
          <h2>Precision solutions for the discerning farmer</h2>
          <p>
            From GPS guidance to land leveling, we provide the technology and expertise
            to help you farm with unprecedented precision.
          </p>
        </div>
      </div>

      <div class="solutions-grid">
        <div class="solution-card">
          <div class="solution-number">01</div>
          <h3 class="solution-title">GPS Guidance</h3>
          <p class="solution-desc">
            Sub-inch precision guidance that reduces overlap and enables 24/7 operation.
          </p>
          <a href="#products" class="solution-link">Explore &#8594;</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">02</div>
          <h3 class="solution-title">Land Leveling</h3>
          <p class="solution-desc">
            3D GPS-based leveling for perfect field grades and optimal water efficiency.
          </p>
          <a href="#products" class="solution-link">Explore &#8594;</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">03</div>
          <h3 class="solution-title">Expert Support</h3>
          <p class="solution-desc">
            Personalized guidance from specialists who understand your operation.
          </p>
          <a href="#contact" class="solution-link">Connect &#8594;</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">04</div>
          <h3 class="solution-title">Farm Software</h3>
          <p class="solution-desc">
            Comprehensive field records and analytics with Blossom platform.
          </p>
          <a href="#products" class="solution-link">Explore &#8594;</a>
        </div>
      </div>
    </section>

    <section id="products" class="products">
      <div class="products-header">
        <h2>The Collection</h2>
        <p>Curated precision equipment for the modern farm</p>
      </div>

      <div class="products-grid">
        <div class="product-card">
          <div class="product-image-wrapper">
            <img src="/images/products/at2-autosteer.png" alt="FJD AT2" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Auto-Steer</div>
            <h3 class="product-name">FJD AT2</h3>
            <p class="product-desc">Advanced auto-steering with centimeter-level accuracy. Universal mount.</p>
            <div class="product-footer">
              <span class="product-price">$7,199</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">
                View &#8594;
              </a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrapper">
            <img src="/images/products/al01-land-leveling.png" alt="FJD AL01" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Land Leveling</div>
            <h3 class="product-name">FJD AL01 3D</h3>
            <p class="product-desc">Precision 3D land leveling for perfect field grades.</p>
            <div class="product-footer">
              <span class="product-price">$6,799</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">
                View &#8594;
              </a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrapper">
            <img src="/images/products/trion-v1t-rover.png" alt="FJD Trion V1t" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">RTK Rover</div>
            <h3 class="product-name">FJD Trion V1t</h3>
            <p class="product-desc">Professional RTK rover for surveying and mapping.</p>
            <div class="product-footer">
              <span class="product-price">$4,249</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">
                View &#8594;
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="about" class="about">
      <div class="about-image-container">
        <img src="/images/hero-field.jpg" alt="Agricultural landscape" class="about-image" />
        <div class="about-image-caption">
          <span>100+</span>
          <p>Farms Equipped</p>
        </div>
      </div>
      <div class="about-content">
        <div class="section-number">02</div>
        <h2>Transforming agriculture through <em>innovation</em></h2>
        <p>
          Elm Lake Labs is dedicated to helping farmers unlock their full potential
          through state-of-the-art precision technology. From small family farms to
          large commercial operations, we provide the tools and expertise you need.
        </p>
        <div class="values-list">
          <div class="value-item">
            <span class="value-number">i.</span>
            <div class="value-content">
              <h4>Innovation</h4>
              <p>Cutting-edge technology backed by rigorous testing</p>
            </div>
          </div>
          <div class="value-item">
            <span class="value-number">ii.</span>
            <div class="value-content">
              <h4>Support</h4>
              <p>Expert guidance from equipment selection to optimization</p>
            </div>
          </div>
          <div class="value-item">
            <span class="value-number">iii.</span>
            <div class="value-content">
              <h4>Excellence</h4>
              <p>Products and service that exceed expectations</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="contact" class="contact">
      <div class="contact-glow left"></div>
      <div class="contact-glow right"></div>
      <div class="contact-inner">
        <h2>Ready to elevate your <em>operation?</em></h2>
        <p>
          Let's discuss how precision agriculture technology can transform your farm.
          Our team is ready to guide you.
        </p>
        <a href="mailto:hello@elmlakelabs.com" class="btn-primary">
          Begin the Conversation
        </a>
      </div>
    </section>

    <footer>
      <a href="/" class="footer-logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <div class="footer-links">
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a href="https://store.elmlakelabs.com" target="_blank">Shop</a>
      </div>
      <span class="footer-copy">&copy; 2024 Elm Lake Labs</span>
    </footer>
  </body>
</html>
