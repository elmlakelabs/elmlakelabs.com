---
/**
 * CONCEPT 10: "KINETIC DATA FLOW"
 * Aesthetic: Data visualization, live systems dashboard, real-time telemetry
 * Feel: High-tech command center, agricultural mission control
 * Typography: System-like mono (Fira Code) + clean variable (Outfit)
 * Colors: Deep midnight blue, electric cyan, data green, warm amber for alerts
 * Unique: Animated data streams, live-updating visuals, telemetry-style layouts
 */
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Elm Lake Labs | Concept 10 - Kinetic Data</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500;600;700&family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
      :root {
        --midnight: #0A0E1A;
        --midnight-light: #121829;
        --midnight-lighter: #1A2138;
        --slate: #2D3654;
        --slate-light: #4A5578;

        --cyan: #00D4FF;
        --cyan-dim: rgba(0, 212, 255, 0.15);
        --green: #00FF88;
        --green-dim: rgba(0, 255, 136, 0.15);
        --amber: #FFB800;
        --amber-dim: rgba(255, 184, 0, 0.15);
        --magenta: #FF00AA;

        --white: #FFFFFF;
        --gray-100: #E5E7EB;
        --gray-400: #9CA3AF;
        --gray-500: #6B7280;

        --font-display: 'Outfit', sans-serif;
        --font-mono: 'Fira Code', monospace;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }

      html { scroll-behavior: smooth; }

      body {
        font-family: var(--font-display);
        background: var(--midnight);
        color: var(--white);
        line-height: 1.5;
      }

      /* Animated grid background */
      body::before {
        content: '';
        position: fixed;
        inset: 0;
        background-image:
          linear-gradient(rgba(0, 212, 255, 0.03) 1px, transparent 1px),
          linear-gradient(90deg, rgba(0, 212, 255, 0.03) 1px, transparent 1px);
        background-size: 50px 50px;
        animation: gridPulse 4s ease-in-out infinite;
        pointer-events: none;
        z-index: 0;
      }

      @keyframes gridPulse {
        0%, 100% { opacity: 0.5; }
        50% { opacity: 1; }
      }

      /* Concept switcher */
      .concept-nav {
        position: fixed;
        top: 20px;
        right: 20px;
        z-index: 1001;
        display: flex;
        gap: 4px;
        padding: 8px;
        background: var(--midnight-light);
        border: 1px solid var(--slate);
        border-radius: 8px;
      }
      .concept-nav a {
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: var(--font-mono);
        font-size: 11px;
        text-decoration: none;
        color: var(--slate-light);
        border-radius: 4px;
        transition: all 0.2s;
      }
      .concept-nav a:hover {
        background: var(--slate);
        color: var(--white);
      }
      .concept-nav a.active {
        background: var(--cyan);
        color: var(--midnight);
      }

      /* Header */
      header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        padding: 0 48px;
        height: 72px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: rgba(10, 14, 26, 0.9);
        backdrop-filter: blur(20px);
        border-bottom: 1px solid var(--slate);
      }

      .logo {
        display: flex;
        align-items: center;
        gap: 16px;
        text-decoration: none;
      }

      .logo img {
        height: 48px;
        width: auto;
      }

      .logo-status {
        display: flex;
        align-items: center;
        gap: 8px;
        font-family: var(--font-mono);
        font-size: 10px;
        color: var(--green);
        padding: 4px 12px;
        background: var(--green-dim);
        border-radius: 100px;
      }

      .logo-status::before {
        content: '';
        width: 6px;
        height: 6px;
        background: var(--green);
        border-radius: 50%;
        animation: blink 2s infinite;
      }

      @keyframes blink {
        0%, 50%, 100% { opacity: 1; }
        25%, 75% { opacity: 0.3; }
      }

      nav {
        display: flex;
        gap: 32px;
      }

      nav a {
        font-family: var(--font-mono);
        font-size: 12px;
        font-weight: 500;
        color: var(--gray-400);
        text-decoration: none;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        transition: color 0.2s;
      }

      nav a:hover { color: var(--cyan); }

      .header-cta {
        font-family: var(--font-mono);
        font-size: 12px;
        font-weight: 600;
        padding: 12px 24px;
        background: var(--cyan);
        color: var(--midnight);
        text-decoration: none;
        border-radius: 6px;
        transition: all 0.2s;
      }

      .header-cta:hover {
        background: var(--white);
        box-shadow: 0 0 20px rgba(0, 212, 255, 0.4);
      }

      /* Hero */
      .hero {
        min-height: 100vh;
        padding: 140px 48px 80px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: center;
        position: relative;
      }

      .hero-content {
        position: relative;
        z-index: 1;
      }

      .hero-terminal {
        font-family: var(--font-mono);
        font-size: 11px;
        color: var(--green);
        margin-bottom: 24px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .hero-terminal::before {
        content: '>';
        color: var(--cyan);
      }

      .typing-cursor {
        display: inline-block;
        width: 8px;
        height: 16px;
        background: var(--green);
        animation: cursor 1s infinite;
      }

      @keyframes cursor {
        0%, 50% { opacity: 1; }
        51%, 100% { opacity: 0; }
      }

      .hero-title {
        font-size: clamp(48px, 7vw, 80px);
        font-weight: 800;
        line-height: 1.05;
        letter-spacing: -0.03em;
        color: var(--white);
        margin-bottom: 32px;
      }

      .hero-title span {
        display: block;
      }

      .hero-title .glow {
        color: var(--cyan);
        text-shadow: 0 0 40px rgba(0, 212, 255, 0.5);
      }

      .hero-desc {
        font-size: 18px;
        font-weight: 400;
        color: var(--gray-400);
        max-width: 480px;
        margin-bottom: 48px;
        line-height: 1.7;
      }

      .hero-ctas {
        display: flex;
        gap: 16px;
      }

      .btn-data {
        font-family: var(--font-mono);
        font-size: 12px;
        font-weight: 600;
        padding: 16px 32px;
        text-decoration: none;
        border-radius: 6px;
        transition: all 0.2s;
        display: inline-flex;
        align-items: center;
        gap: 12px;
      }

      .btn-data.primary {
        background: var(--cyan);
        color: var(--midnight);
      }

      .btn-data.primary:hover {
        box-shadow: 0 0 30px rgba(0, 212, 255, 0.5);
        transform: translateY(-2px);
      }

      .btn-data.secondary {
        background: var(--midnight-lighter);
        color: var(--white);
        border: 1px solid var(--slate);
      }

      .btn-data.secondary:hover {
        border-color: var(--cyan);
      }

      .hero-visual {
        position: relative;
      }

      .data-dashboard {
        background: var(--midnight-light);
        border: 1px solid var(--slate);
        border-radius: 12px;
        overflow: hidden;
      }

      .dashboard-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 16px 20px;
        background: var(--midnight-lighter);
        border-bottom: 1px solid var(--slate);
      }

      .dashboard-title {
        font-family: var(--font-mono);
        font-size: 12px;
        color: var(--gray-400);
        text-transform: uppercase;
        letter-spacing: 0.1em;
      }

      .dashboard-status {
        display: flex;
        align-items: center;
        gap: 8px;
        font-family: var(--font-mono);
        font-size: 10px;
        color: var(--green);
      }

      .dashboard-status::before {
        content: '';
        width: 6px;
        height: 6px;
        background: var(--green);
        border-radius: 50%;
        animation: blink 2s infinite;
      }

      .dashboard-content {
        padding: 24px;
      }

      .data-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 16px;
      }

      .data-card {
        background: var(--midnight);
        border: 1px solid var(--slate);
        border-radius: 8px;
        padding: 20px;
        position: relative;
        overflow: hidden;
      }

      .data-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: var(--cyan);
      }

      .data-card.green::before { background: var(--green); }
      .data-card.amber::before { background: var(--amber); }
      .data-card.magenta::before { background: var(--magenta); }

      .data-label {
        font-family: var(--font-mono);
        font-size: 10px;
        color: var(--gray-500);
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 8px;
      }

      .data-value {
        font-family: var(--font-mono);
        font-size: 32px;
        font-weight: 700;
        color: var(--white);
        line-height: 1;
      }

      .data-value.cyan { color: var(--cyan); }
      .data-value.green { color: var(--green); }
      .data-value.amber { color: var(--amber); }

      .data-trend {
        font-family: var(--font-mono);
        font-size: 11px;
        color: var(--green);
        margin-top: 8px;
        display: flex;
        align-items: center;
        gap: 4px;
      }

      .data-trend.down { color: var(--amber); }

      .data-visual {
        grid-column: 1 / -1;
        background: var(--midnight);
        border: 1px solid var(--slate);
        border-radius: 8px;
        padding: 20px;
        height: 120px;
        position: relative;
        overflow: hidden;
      }

      .wave-line {
        position: absolute;
        bottom: 20px;
        left: 0;
        right: 0;
        height: 60px;
        background: linear-gradient(180deg, transparent, var(--cyan-dim));
        clip-path: polygon(
          0% 60%, 5% 40%, 10% 50%, 15% 30%, 20% 45%, 25% 25%, 30% 40%,
          35% 20%, 40% 35%, 45% 15%, 50% 30%, 55% 10%, 60% 25%, 65% 5%,
          70% 20%, 75% 10%, 80% 25%, 85% 15%, 90% 30%, 95% 20%, 100% 35%,
          100% 100%, 0% 100%
        );
        animation: wave 3s ease-in-out infinite;
      }

      @keyframes wave {
        0%, 100% { transform: translateX(0); }
        50% { transform: translateX(-20px); }
      }

      .wave-line::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 2px;
        background: var(--cyan);
        clip-path: polygon(
          0% 60%, 5% 40%, 10% 50%, 15% 30%, 20% 45%, 25% 25%, 30% 40%,
          35% 20%, 40% 35%, 45% 15%, 50% 30%, 55% 10%, 60% 25%, 65% 5%,
          70% 20%, 75% 10%, 80% 25%, 85% 15%, 90% 30%, 95% 20%, 100% 35%
        );
      }

      /* Stats */
      .stats {
        background: var(--midnight-light);
        border-top: 1px solid var(--slate);
        border-bottom: 1px solid var(--slate);
        padding: 48px;
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 32px;
      }

      .stat {
        text-align: center;
        padding: 24px;
        background: var(--midnight);
        border: 1px solid var(--slate);
        border-radius: 8px;
        position: relative;
        overflow: hidden;
      }

      .stat::before {
        content: '';
        position: absolute;
        top: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 40px;
        height: 3px;
        background: var(--cyan);
        border-radius: 0 0 2px 2px;
      }

      .stat-value {
        font-family: var(--font-mono);
        font-size: 40px;
        font-weight: 700;
        color: var(--cyan);
        line-height: 1;
      }

      .stat-label {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--gray-500);
        margin-top: 12px;
      }

      /* Solutions */
      .solutions {
        padding: 120px 48px;
        background: var(--midnight);
        position: relative;
      }

      .section-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        margin-bottom: 80px;
        padding-bottom: 32px;
        border-bottom: 1px solid var(--slate);
      }

      .section-meta {
        font-family: var(--font-mono);
        font-size: 11px;
        color: var(--cyan);
        text-transform: uppercase;
        letter-spacing: 0.15em;
        margin-bottom: 16px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .section-meta::before {
        content: '//';
        color: var(--slate-light);
      }

      .section-title {
        font-size: clamp(36px, 5vw, 56px);
        font-weight: 800;
        color: var(--white);
        letter-spacing: -0.02em;
      }

      .section-number {
        font-family: var(--font-mono);
        font-size: 72px;
        font-weight: 700;
        color: var(--midnight-lighter);
        line-height: 1;
      }

      .solutions-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 20px;
      }

      .solution-card {
        background: var(--midnight-light);
        border: 1px solid var(--slate);
        border-radius: 12px;
        padding: 32px;
        transition: all 0.3s;
        position: relative;
        overflow: hidden;
      }

      .solution-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 0;
        background: linear-gradient(180deg, var(--cyan-dim), transparent);
        transition: height 0.3s;
      }

      .solution-card:hover {
        border-color: var(--cyan);
        transform: translateY(-4px);
      }

      .solution-card:hover::before {
        height: 100%;
      }

      .solution-icon {
        width: 48px;
        height: 48px;
        background: var(--cyan-dim);
        border: 1px solid var(--cyan);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 24px;
        position: relative;
      }

      .solution-icon svg {
        width: 24px;
        height: 24px;
        stroke: var(--cyan);
      }

      .solution-number {
        font-family: var(--font-mono);
        font-size: 11px;
        color: var(--slate-light);
        margin-bottom: 16px;
        position: relative;
      }

      .solution-title {
        font-size: 20px;
        font-weight: 700;
        color: var(--white);
        margin-bottom: 12px;
        position: relative;
      }

      .solution-desc {
        font-size: 14px;
        color: var(--gray-400);
        line-height: 1.6;
        margin-bottom: 24px;
        position: relative;
      }

      .solution-link {
        font-family: var(--font-mono);
        font-size: 11px;
        color: var(--cyan);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: gap 0.2s;
        position: relative;
      }

      .solution-link:hover { gap: 12px; }

      /* Products */
      .products {
        padding: 120px 48px;
        background: var(--midnight-light);
      }

      .products-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 24px;
        margin-top: 80px;
      }

      .product-card {
        background: var(--midnight);
        border: 1px solid var(--slate);
        border-radius: 12px;
        overflow: hidden;
        transition: all 0.3s;
      }

      .product-card:hover {
        border-color: var(--cyan);
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        transform: translateY(-8px);
      }

      .product-image-wrap {
        aspect-ratio: 4/3;
        background: linear-gradient(135deg, var(--midnight-lighter), var(--midnight));
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 40px;
        position: relative;
        border-bottom: 1px solid var(--slate);
      }

      .product-badge {
        position: absolute;
        top: 16px;
        left: 16px;
        font-family: var(--font-mono);
        font-size: 9px;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        padding: 6px 12px;
        background: var(--green);
        color: var(--midnight);
        border-radius: 4px;
      }

      .product-image {
        max-width: 75%;
        max-height: 140px;
        object-fit: contain;
        transition: transform 0.3s;
      }

      .product-card:hover .product-image {
        transform: scale(1.08);
      }

      .product-content {
        padding: 28px;
      }

      .product-category {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--gray-500);
        margin-bottom: 8px;
      }

      .product-name {
        font-size: 24px;
        font-weight: 700;
        color: var(--white);
        margin-bottom: 12px;
      }

      .product-desc {
        font-size: 14px;
        color: var(--gray-400);
        line-height: 1.6;
        margin-bottom: 24px;
      }

      .product-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 20px;
        border-top: 1px solid var(--slate);
      }

      .product-price {
        font-family: var(--font-mono);
        font-size: 24px;
        font-weight: 700;
        color: var(--cyan);
      }

      .product-link {
        font-family: var(--font-mono);
        font-size: 11px;
        color: var(--gray-400);
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 6px;
        transition: color 0.2s;
      }

      .product-link:hover { color: var(--cyan); }

      /* About */
      .about {
        padding: 120px 48px;
        background: var(--midnight);
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 100px;
        align-items: center;
      }

      .about-visual {
        position: relative;
      }

      .about-image-frame {
        position: relative;
        border-radius: 12px;
        overflow: hidden;
        border: 1px solid var(--slate);
      }

      .about-image {
        width: 100%;
        aspect-ratio: 4/3;
        object-fit: cover;
      }

      .about-overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, var(--cyan-dim), transparent);
        pointer-events: none;
      }

      .about-badge {
        position: absolute;
        bottom: -24px;
        right: 32px;
        background: var(--cyan);
        color: var(--midnight);
        padding: 24px 32px;
        border-radius: 8px;
        text-align: center;
      }

      .about-badge-value {
        font-family: var(--font-mono);
        font-size: 36px;
        font-weight: 700;
        line-height: 1;
      }

      .about-badge-label {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        margin-top: 4px;
      }

      .about-content .section-title {
        margin-bottom: 24px;
      }

      .about-desc {
        font-size: 18px;
        color: var(--gray-400);
        line-height: 1.8;
        margin-bottom: 40px;
      }

      .values-list {
        display: flex;
        flex-direction: column;
        gap: 12px;
      }

      .value-item {
        display: flex;
        align-items: center;
        gap: 16px;
        padding: 16px 20px;
        background: var(--midnight-light);
        border: 1px solid var(--slate);
        border-radius: 8px;
        transition: all 0.2s;
      }

      .value-item:hover {
        border-color: var(--cyan);
      }

      .value-icon {
        width: 40px;
        height: 40px;
        background: var(--cyan-dim);
        border: 1px solid var(--cyan);
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
      }

      .value-icon svg {
        width: 20px;
        height: 20px;
        stroke: var(--cyan);
      }

      .value-text {
        font-size: 15px;
        font-weight: 500;
        color: var(--white);
      }

      /* Contact */
      .contact {
        padding: 120px 48px;
        background: linear-gradient(180deg, var(--midnight-light), var(--midnight));
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .contact::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 600px;
        height: 600px;
        background: radial-gradient(circle, var(--cyan-dim) 0%, transparent 70%);
        transform: translate(-50%, -50%);
        pointer-events: none;
      }

      .contact-content {
        position: relative;
        z-index: 1;
      }

      .contact-title {
        font-size: clamp(36px, 6vw, 64px);
        font-weight: 800;
        color: var(--white);
        margin-bottom: 20px;
        letter-spacing: -0.02em;
      }

      .contact-title span {
        color: var(--cyan);
        text-shadow: 0 0 40px rgba(0, 212, 255, 0.5);
      }

      .contact-desc {
        font-size: 18px;
        color: var(--gray-400);
        max-width: 480px;
        margin: 0 auto 40px;
      }

      /* Footer */
      footer {
        background: var(--midnight);
        padding: 48px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 1px solid var(--slate);
      }

      .footer-logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .footer-logo img {
        height: 36px;
        width: auto;
      }

      .footer-links {
        display: flex;
        gap: 40px;
      }

      .footer-links a {
        font-family: var(--font-mono);
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--gray-500);
        text-decoration: none;
        transition: color 0.2s;
      }

      .footer-links a:hover { color: var(--cyan); }

      .footer-copy {
        font-family: var(--font-mono);
        font-size: 11px;
        color: var(--gray-500);
      }

      /* Animations */
      @keyframes slideUp {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
      }

      .animate-in {
        animation: slideUp 0.7s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      }

      .delay-1 { animation-delay: 0.1s; opacity: 0; }
      .delay-2 { animation-delay: 0.2s; opacity: 0; }
      .delay-3 { animation-delay: 0.3s; opacity: 0; }
      .delay-4 { animation-delay: 0.4s; opacity: 0; }

      /* Responsive */
      @media (max-width: 1024px) {
        header { padding: 0 24px; }
        nav { display: none; }
        .hero {
          grid-template-columns: 1fr;
          padding: 120px 24px 80px;
        }
        .hero-visual { display: none; }
        .stats { grid-template-columns: repeat(2, 1fr); }
        .solutions, .products, .about, .contact { padding: 80px 24px; }
        .solutions-grid { grid-template-columns: repeat(2, 1fr); }
        .products-grid { grid-template-columns: 1fr; }
        .about { grid-template-columns: 1fr; gap: 60px; }
        footer { flex-direction: column; gap: 24px; text-align: center; padding: 40px 24px; }
      }

      @media (max-width: 640px) {
        .solutions-grid { grid-template-columns: 1fr; }
        .data-grid { grid-template-columns: 1fr; }
      }
    </style>
  </head>
  <body>
    <div class="concept-nav">
      <a href="/" title="Original">⌂</a>
      <a href="/concept-1" title="Soft Gradient">1</a>
      <a href="/concept-2" title="Neon Grid">2</a>
      <a href="/concept-3" title="Aurora Flow">3</a>
      <a href="/concept-4" title="Heavy Metal">4</a>
      <a href="/concept-5" title="Editorial">5</a>
      <a href="/concept-6" title="Brutalist">6</a>
      <a href="/concept-7" title="Blueprint">7</a>
      <a href="/concept-8" title="Retro">8</a>
      <a href="/concept-9" title="Swiss">9</a>
      <a href="/concept-10" class="active" title="Kinetic">10</a>
    </div>

    <header>
      <a href="/" class="logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
        <span class="logo-status">Systems Online</span>
      </a>
      <nav>
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
      <a href="https://store.elmlakelabs.com" class="header-cta" target="_blank">Shop Now</a>
    </header>

    <section class="hero">
      <div class="hero-content">
        <div class="hero-terminal animate-in">
          initializing precision_agriculture_systems<span class="typing-cursor"></span>
        </div>
        <h1 class="hero-title animate-in delay-1">
          <span>Real-Time</span>
          <span>Precision</span>
          <span class="glow">Agriculture</span>
        </h1>
        <p class="hero-desc animate-in delay-2">
          Live GPS telemetry, auto-steer systems, and farm analytics
          that transform raw data into actionable insights.
        </p>
        <div class="hero-ctas animate-in delay-3">
          <a href="#products" class="btn-data primary">
            View Systems
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a href="#about" class="btn-data secondary">Learn More</a>
        </div>
      </div>
      <div class="hero-visual animate-in delay-4">
        <div class="data-dashboard">
          <div class="dashboard-header">
            <span class="dashboard-title">Field Telemetry</span>
            <span class="dashboard-status">Live Data</span>
          </div>
          <div class="dashboard-content">
            <div class="data-grid">
              <div class="data-card">
                <div class="data-label">RTK Accuracy</div>
                <div class="data-value cyan">±1"</div>
                <div class="data-trend">↑ Optimal</div>
              </div>
              <div class="data-card green">
                <div class="data-label">Farms Online</div>
                <div class="data-value green">100+</div>
                <div class="data-trend">↑ Active</div>
              </div>
              <div class="data-card amber">
                <div class="data-label">Support Uptime</div>
                <div class="data-value amber">24/7</div>
                <div class="data-trend">● Available</div>
              </div>
              <div class="data-card magenta">
                <div class="data-label">Fuel Savings</div>
                <div class="data-value">15%</div>
                <div class="data-trend">↑ Avg. reduction</div>
              </div>
              <div class="data-visual">
                <div class="wave-line"></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <div class="stats">
      <div class="stat">
        <div class="stat-value">100+</div>
        <div class="stat-label">Farms Connected</div>
      </div>
      <div class="stat">
        <div class="stat-value">±1"</div>
        <div class="stat-label">RTK Accuracy</div>
      </div>
      <div class="stat">
        <div class="stat-value">24/7</div>
        <div class="stat-label">Live Support</div>
      </div>
      <div class="stat">
        <div class="stat-value">15%</div>
        <div class="stat-label">Fuel Reduction</div>
      </div>
    </div>

    <section id="solutions" class="solutions">
      <div class="section-header">
        <div>
          <span class="section-meta">System Modules</span>
          <h2 class="section-title">Solutions</h2>
        </div>
        <div class="section-number">01</div>
      </div>

      <div class="solutions-grid">
        <div class="solution-card">
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <circle cx="12" cy="12" r="10"/>
              <circle cx="12" cy="12" r="3"/>
              <path d="M12 2v4M12 18v4M2 12h4M18 12h4"/>
            </svg>
          </div>
          <div class="solution-number">// MODULE_01</div>
          <h3 class="solution-title">GPS Guidance</h3>
          <p class="solution-desc">Sub-inch precision positioning for reduced overlap and 24/7 field operation.</p>
          <a href="#products" class="solution-link">Access module →</a>
        </div>
        <div class="solution-card">
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <rect x="3" y="3" width="18" height="18" rx="2"/>
              <path d="M3 9h18M9 9v12"/>
            </svg>
          </div>
          <div class="solution-number">// MODULE_02</div>
          <h3 class="solution-title">Land Leveling</h3>
          <p class="solution-desc">3D GPS-based terrain control for optimal field grades.</p>
          <a href="#products" class="solution-link">Access module →</a>
        </div>
        <div class="solution-card">
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <div class="solution-number">// MODULE_03</div>
          <h3 class="solution-title">Live Support</h3>
          <p class="solution-desc">Real-time assistance from precision agriculture specialists.</p>
          <a href="#contact" class="solution-link">Connect →</a>
        </div>
        <div class="solution-card">
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <rect x="2" y="3" width="20" height="14" rx="2"/>
              <path d="M8 21h8M12 17v4"/>
            </svg>
          </div>
          <div class="solution-number">// MODULE_04</div>
          <h3 class="solution-title">Analytics Hub</h3>
          <p class="solution-desc">Blossom: centralized field data, tracking, and insights.</p>
          <a href="#products" class="solution-link">Access module →</a>
        </div>
      </div>
    </section>

    <section id="products" class="products">
      <div class="section-header">
        <div>
          <span class="section-meta">Hardware Catalog</span>
          <h2 class="section-title">Products</h2>
        </div>
        <div class="section-number">02</div>
      </div>

      <div class="products-grid">
        <div class="product-card">
          <div class="product-image-wrap">
            <div class="product-badge">Primary</div>
            <img src="/images/products/at2-autosteer.png" alt="FJD AT2" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Auto-Steer System</div>
            <h3 class="product-name">FJD AT2</h3>
            <p class="product-desc">Advanced auto-steering with real-time centimeter-level accuracy.</p>
            <div class="product-footer">
              <span class="product-price">$7,199</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Deploy →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrap">
            <img src="/images/products/al01-land-leveling.png" alt="FJD AL01" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Land Leveling</div>
            <h3 class="product-name">FJD AL01 3D</h3>
            <p class="product-desc">Precision 3D leveling for optimized field grading.</p>
            <div class="product-footer">
              <span class="product-price">$6,799</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Deploy →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrap">
            <img src="/images/products/trion-v1t-rover.png" alt="FJD Trion V1t" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">RTK Rover</div>
            <h3 class="product-name">FJD Trion V1t</h3>
            <p class="product-desc">Professional RTK rover for surveying and mapping.</p>
            <div class="product-footer">
              <span class="product-price">$4,249</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Deploy →</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="about" class="about">
      <div class="about-visual">
        <div class="about-image-frame">
          <img src="/images/hero-field.jpg" alt="Field operations" class="about-image" />
          <div class="about-overlay"></div>
        </div>
        <div class="about-badge">
          <div class="about-badge-value">100+</div>
          <div class="about-badge-label">Farms Online</div>
        </div>
      </div>
      <div class="about-content">
        <span class="section-meta">System Origin</span>
        <h2 class="section-title">Our Mission</h2>
        <p class="about-desc">
          Elm Lake Labs builds the data infrastructure that powers modern farming.
          We transform precision agriculture from reactive to predictive—helping
          farmers make smarter decisions in real-time.
        </p>
        <div class="values-list">
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </div>
            <span class="value-text">Real-time data processing</span>
          </div>
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              </svg>
            </div>
            <span class="value-text">99.9% system uptime</span>
          </div>
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <path d="M12 6v6l4 2"/>
              </svg>
            </div>
            <span class="value-text">24/7 live support</span>
          </div>
        </div>
      </div>
    </section>

    <section id="contact" class="contact">
      <div class="contact-content">
        <h2 class="contact-title">Ready to <span>Connect?</span></h2>
        <p class="contact-desc">
          Initialize your precision agriculture journey with Elm Lake Labs.
        </p>
        <a href="mailto:hello@elmlakelabs.com" class="btn-data primary">
          Initiate Contact
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </section>

    <footer>
      <a href="/" class="footer-logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <div class="footer-links">
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a href="https://store.elmlakelabs.com" target="_blank">Shop</a>
      </div>
      <span class="footer-copy">© 2024 Elm Lake Labs</span>
    </footer>
  </body>
</html>
