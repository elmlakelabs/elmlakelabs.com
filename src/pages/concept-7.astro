---
/**
 * CONCEPT 7: "ORGANIC BLUEPRINT"
 * Aesthetic: Technical drawings meets living nature, engineering meets growth
 * Feel: Precision with soul, like a botanist's laboratory notebook
 * Typography: Technical serif (Cormorant) + precise mono (JetBrains Mono)
 * Colors: Cream paper, forest ink, blueprint cyan, specimen green
 * Unique: Hand-drawn line overlays, botanical illustration style, technical annotations
 */
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Elm Lake Labs | Concept 7 - Organic Blueprint</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
      :root {
        --paper: #F7F4ED;
        --paper-dark: #EDE9DE;
        --cream: #FBF9F3;
        --ink: #1A2E1F;
        --ink-light: #2D4A35;
        --ink-muted: #5A7360;
        --ink-pale: #8BA393;
        --blueprint: #1E5F74;
        --blueprint-light: #2B7A8C;
        --specimen: #3D6B4F;
        --specimen-light: #5A8F6A;
        --annotation: #8B4513;
        --grid-line: rgba(26, 46, 31, 0.08);

        --font-serif: 'Cormorant Garamond', Georgia, serif;
        --font-mono: 'JetBrains Mono', monospace;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }

      html { scroll-behavior: smooth; }

      body {
        font-family: var(--font-serif);
        background: var(--paper);
        color: var(--ink);
        line-height: 1.6;
        position: relative;
      }

      /* Paper texture */
      body::before {
        content: '';
        position: fixed;
        inset: 0;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='paper'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.04' numOctaves='5' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23paper)'/%3E%3C/svg%3E");
        opacity: 0.03;
        pointer-events: none;
        z-index: 1000;
      }

      /* Blueprint grid pattern */
      .grid-bg {
        position: fixed;
        inset: 0;
        background-image:
          linear-gradient(var(--grid-line) 1px, transparent 1px),
          linear-gradient(90deg, var(--grid-line) 1px, transparent 1px);
        background-size: 40px 40px;
        pointer-events: none;
        z-index: -1;
      }

      /* Concept switcher */
      .concept-nav {
        position: fixed;
        top: 20px;
        right: 20px;
        z-index: 1001;
        display: flex;
        gap: 4px;
        padding: 8px 10px;
        background: var(--cream);
        border: 1px solid var(--ink-pale);
        border-radius: 2px;
      }
      .concept-nav a {
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: var(--font-mono);
        font-size: 11px;
        text-decoration: none;
        color: var(--ink-muted);
        border: 1px solid transparent;
        transition: all 0.2s;
      }
      .concept-nav a:hover {
        border-color: var(--ink-pale);
        color: var(--ink);
      }
      .concept-nav a.active {
        background: var(--specimen);
        color: var(--cream);
        border-color: var(--specimen);
      }

      /* Header */
      header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        padding: 0 60px;
        height: 80px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: var(--cream);
        border-bottom: 1px solid var(--ink-pale);
      }

      .logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .logo img {
        height: 48px;
        width: auto;
        /* Invert white logo for light background, keep gradient colors */
        filter: brightness(0) saturate(100%) invert(12%) sepia(8%) saturate(1200%) hue-rotate(80deg);
      }

      nav {
        display: flex;
        gap: 40px;
      }

      nav a {
        font-family: var(--font-mono);
        font-size: 11px;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--ink-muted);
        text-decoration: none;
        position: relative;
        transition: color 0.2s;
      }

      nav a::after {
        content: '';
        position: absolute;
        bottom: -4px;
        left: 0;
        width: 0;
        height: 1px;
        background: var(--specimen);
        transition: width 0.3s;
      }

      nav a:hover { color: var(--ink); }
      nav a:hover::after { width: 100%; }

      .header-cta {
        font-family: var(--font-mono);
        font-size: 11px;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        padding: 12px 24px;
        background: var(--ink);
        color: var(--cream);
        text-decoration: none;
        border: 1px solid var(--ink);
        transition: all 0.3s;
      }

      .header-cta:hover {
        background: transparent;
        color: var(--ink);
      }

      /* Hero */
      .hero {
        min-height: 100vh;
        padding: 140px 60px 100px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: center;
        position: relative;
      }

      .hero-content {
        position: relative;
      }

      .hero-specimen {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--annotation);
        margin-bottom: 24px;
        display: flex;
        align-items: center;
        gap: 16px;
      }

      .hero-specimen::before {
        content: '№ 001';
        padding: 4px 8px;
        border: 1px solid var(--annotation);
      }

      .hero-title {
        font-family: var(--font-serif);
        font-size: clamp(48px, 7vw, 80px);
        font-weight: 400;
        font-style: italic;
        line-height: 1.1;
        color: var(--ink);
        margin-bottom: 32px;
        position: relative;
      }

      .hero-title span {
        display: block;
      }

      .hero-title .accent {
        color: var(--specimen);
        font-style: normal;
        font-weight: 600;
      }

      .hero-annotation {
        position: absolute;
        right: -120px;
        top: 50%;
        font-family: var(--font-mono);
        font-size: 9px;
        color: var(--ink-muted);
        writing-mode: vertical-rl;
        text-orientation: mixed;
        letter-spacing: 0.1em;
      }

      .hero-desc {
        font-size: 20px;
        color: var(--ink-light);
        max-width: 480px;
        margin-bottom: 48px;
        line-height: 1.7;
      }

      .hero-ctas {
        display: flex;
        gap: 20px;
        align-items: center;
      }

      .btn-organic {
        font-family: var(--font-mono);
        font-size: 11px;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        padding: 16px 32px;
        text-decoration: none;
        transition: all 0.3s;
        display: inline-flex;
        align-items: center;
        gap: 12px;
      }

      .btn-organic.primary {
        background: var(--specimen);
        color: var(--cream);
        border: 1px solid var(--specimen);
      }

      .btn-organic.primary:hover {
        background: var(--ink);
        border-color: var(--ink);
      }

      .btn-organic.secondary {
        background: transparent;
        color: var(--ink);
        border: 1px solid var(--ink-pale);
      }

      .btn-organic.secondary:hover {
        border-color: var(--ink);
      }

      .hero-visual {
        position: relative;
        aspect-ratio: 1;
      }

      .hero-diagram {
        position: absolute;
        inset: 0;
        border: 1px solid var(--ink-pale);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .hero-diagram::before {
        content: '';
        position: absolute;
        inset: 20px;
        border: 1px dashed var(--ink-pale);
        border-radius: 50%;
      }

      .hero-diagram::after {
        content: '';
        position: absolute;
        inset: 60px;
        border: 1px solid var(--ink-pale);
        border-radius: 50%;
      }

      .hero-image-frame {
        width: 70%;
        height: 70%;
        position: relative;
        overflow: hidden;
      }

      .hero-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        filter: sepia(20%) saturate(80%);
      }

      .diagram-label {
        position: absolute;
        font-family: var(--font-mono);
        font-size: 9px;
        color: var(--ink-muted);
        letter-spacing: 0.1em;
      }

      .diagram-label.top { top: -24px; left: 50%; transform: translateX(-50%); }
      .diagram-label.right { right: -80px; top: 50%; transform: translateY(-50%); }
      .diagram-label.bottom { bottom: -24px; left: 50%; transform: translateX(-50%); }
      .diagram-label.left { left: -80px; top: 50%; transform: translateY(-50%); }

      .diagram-dot {
        position: absolute;
        width: 8px;
        height: 8px;
        background: var(--specimen);
        border-radius: 50%;
        border: 2px solid var(--cream);
      }

      .diagram-dot.n { top: -4px; left: 50%; transform: translateX(-50%); }
      .diagram-dot.e { right: -4px; top: 50%; transform: translateY(-50%); }
      .diagram-dot.s { bottom: -4px; left: 50%; transform: translateX(-50%); }
      .diagram-dot.w { left: -4px; top: 50%; transform: translateY(-50%); }

      /* Stats */
      .stats {
        padding: 60px;
        background: var(--cream);
        border-top: 1px solid var(--ink-pale);
        border-bottom: 1px solid var(--ink-pale);
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 40px;
      }

      .stat {
        text-align: center;
        position: relative;
        padding: 24px 0;
      }

      .stat:not(:last-child)::after {
        content: '';
        position: absolute;
        right: -20px;
        top: 50%;
        transform: translateY(-50%);
        width: 1px;
        height: 60px;
        background: var(--ink-pale);
      }

      .stat-value {
        font-family: var(--font-serif);
        font-size: 48px;
        font-weight: 600;
        color: var(--specimen);
        line-height: 1;
      }

      .stat-label {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--ink-muted);
        margin-top: 12px;
      }

      /* Solutions */
      .solutions {
        padding: 120px 60px;
        position: relative;
      }

      .section-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        margin-bottom: 80px;
        padding-bottom: 32px;
        border-bottom: 1px solid var(--ink-pale);
      }

      .section-meta {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--annotation);
        margin-bottom: 16px;
        display: flex;
        align-items: center;
        gap: 12px;
      }

      .section-meta::before {
        content: '';
        width: 24px;
        height: 1px;
        background: var(--annotation);
      }

      .section-title {
        font-family: var(--font-serif);
        font-size: clamp(36px, 5vw, 56px);
        font-weight: 400;
        font-style: italic;
        color: var(--ink);
      }

      .section-number {
        font-family: var(--font-mono);
        font-size: 72px;
        color: var(--paper-dark);
        line-height: 1;
      }

      .solutions-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 40px;
      }

      .solution-card {
        padding: 48px;
        background: var(--cream);
        border: 1px solid var(--ink-pale);
        position: relative;
        transition: all 0.3s;
      }

      .solution-card::before {
        content: '';
        position: absolute;
        top: 16px;
        left: 16px;
        right: 16px;
        bottom: 16px;
        border: 1px dashed var(--ink-pale);
        opacity: 0;
        transition: opacity 0.3s;
        pointer-events: none;
      }

      .solution-card:hover::before {
        opacity: 1;
      }

      .solution-number {
        font-family: var(--font-mono);
        font-size: 10px;
        color: var(--annotation);
        margin-bottom: 24px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .solution-number::after {
        content: '';
        flex: 1;
        height: 1px;
        background: var(--ink-pale);
      }

      .solution-icon {
        width: 48px;
        height: 48px;
        border: 1px solid var(--ink-pale);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 24px;
        transition: all 0.3s;
      }

      .solution-card:hover .solution-icon {
        background: var(--specimen);
        border-color: var(--specimen);
      }

      .solution-icon svg {
        width: 24px;
        height: 24px;
        stroke: var(--ink-muted);
        transition: stroke 0.3s;
      }

      .solution-card:hover .solution-icon svg {
        stroke: var(--cream);
      }

      .solution-title {
        font-family: var(--font-serif);
        font-size: 28px;
        font-weight: 500;
        font-style: italic;
        color: var(--ink);
        margin-bottom: 16px;
      }

      .solution-desc {
        font-size: 16px;
        color: var(--ink-muted);
        line-height: 1.7;
        margin-bottom: 24px;
      }

      .solution-link {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--specimen);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: gap 0.2s;
      }

      .solution-link:hover { gap: 12px; }

      /* Products */
      .products {
        padding: 120px 60px;
        background: var(--cream);
        border-top: 1px solid var(--ink-pale);
      }

      .products-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 40px;
        margin-top: 80px;
      }

      .product-card {
        background: var(--paper);
        border: 1px solid var(--ink-pale);
        transition: all 0.3s;
        position: relative;
      }

      .product-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 20px 40px rgba(26, 46, 31, 0.1);
      }

      .product-specimen {
        position: absolute;
        top: 16px;
        left: 16px;
        font-family: var(--font-mono);
        font-size: 9px;
        color: var(--annotation);
        z-index: 2;
      }

      .product-image-wrap {
        aspect-ratio: 4/3;
        padding: 48px;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        overflow: hidden;
      }

      .product-image-wrap::before {
        content: '';
        position: absolute;
        inset: 24px;
        border: 1px dashed var(--ink-pale);
        border-radius: 50%;
      }

      .product-image {
        max-width: 75%;
        max-height: 140px;
        object-fit: contain;
        position: relative;
        z-index: 1;
        transition: transform 0.3s;
      }

      .product-card:hover .product-image {
        transform: scale(1.08);
      }

      .product-content {
        padding: 32px;
        border-top: 1px solid var(--ink-pale);
      }

      .product-category {
        font-family: var(--font-mono);
        font-size: 9px;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--ink-muted);
        margin-bottom: 8px;
      }

      .product-name {
        font-family: var(--font-serif);
        font-size: 26px;
        font-weight: 500;
        font-style: italic;
        color: var(--ink);
        margin-bottom: 12px;
      }

      .product-desc {
        font-size: 14px;
        color: var(--ink-muted);
        line-height: 1.6;
        margin-bottom: 24px;
      }

      .product-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 20px;
        border-top: 1px solid var(--ink-pale);
      }

      .product-price {
        font-family: var(--font-serif);
        font-size: 28px;
        font-weight: 600;
        color: var(--specimen);
      }

      .product-link {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--ink-muted);
        text-decoration: none;
        transition: color 0.2s;
      }

      .product-link:hover { color: var(--specimen); }

      /* About */
      .about {
        padding: 120px 60px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 100px;
        align-items: center;
        border-top: 1px solid var(--ink-pale);
      }

      .about-visual {
        position: relative;
      }

      .about-frame {
        position: relative;
        padding: 24px;
        border: 1px solid var(--ink-pale);
      }

      .about-frame::before {
        content: 'Fig. 01';
        position: absolute;
        top: -12px;
        left: 24px;
        font-family: var(--font-mono);
        font-size: 10px;
        color: var(--annotation);
        background: var(--paper);
        padding: 0 8px;
      }

      .about-image {
        width: 100%;
        aspect-ratio: 4/3;
        object-fit: cover;
        filter: sepia(15%) saturate(85%);
      }

      .about-caption {
        margin-top: 16px;
        font-family: var(--font-mono);
        font-size: 10px;
        color: var(--ink-muted);
        text-align: center;
        font-style: italic;
      }

      .about-badge {
        position: absolute;
        bottom: -24px;
        right: 48px;
        background: var(--specimen);
        color: var(--cream);
        padding: 24px 32px;
        text-align: center;
      }

      .about-badge-value {
        font-family: var(--font-serif);
        font-size: 36px;
        font-weight: 600;
        line-height: 1;
      }

      .about-badge-label {
        font-family: var(--font-mono);
        font-size: 9px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        opacity: 0.8;
        margin-top: 4px;
      }

      .about-content .section-title {
        margin-bottom: 24px;
      }

      .about-desc {
        font-size: 18px;
        color: var(--ink-light);
        line-height: 1.8;
        margin-bottom: 40px;
      }

      .values-list {
        display: flex;
        flex-direction: column;
        gap: 16px;
      }

      .value-item {
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 20px 24px;
        background: var(--cream);
        border: 1px solid var(--ink-pale);
        transition: all 0.2s;
      }

      .value-item:hover {
        border-color: var(--specimen);
      }

      .value-icon {
        width: 40px;
        height: 40px;
        border: 1px solid var(--ink-pale);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        transition: all 0.2s;
      }

      .value-item:hover .value-icon {
        background: var(--specimen);
        border-color: var(--specimen);
      }

      .value-icon svg {
        width: 20px;
        height: 20px;
        stroke: var(--ink-muted);
        transition: stroke 0.2s;
      }

      .value-item:hover .value-icon svg {
        stroke: var(--cream);
      }

      .value-text {
        font-family: var(--font-serif);
        font-size: 16px;
        font-style: italic;
        color: var(--ink);
      }

      /* Contact */
      .contact {
        padding: 120px 60px;
        background: var(--ink);
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .contact::before {
        content: '';
        position: absolute;
        inset: 0;
        background-image:
          linear-gradient(rgba(247, 244, 237, 0.03) 1px, transparent 1px),
          linear-gradient(90deg, rgba(247, 244, 237, 0.03) 1px, transparent 1px);
        background-size: 40px 40px;
      }

      .contact-content {
        position: relative;
        z-index: 1;
      }

      .contact-title {
        font-family: var(--font-serif);
        font-size: clamp(36px, 6vw, 64px);
        font-weight: 400;
        font-style: italic;
        color: var(--cream);
        margin-bottom: 20px;
      }

      .contact-title span {
        color: var(--specimen-light);
        font-style: normal;
        font-weight: 600;
      }

      .contact-desc {
        font-size: 18px;
        color: var(--ink-pale);
        max-width: 480px;
        margin: 0 auto 40px;
      }

      .contact .btn-organic.primary {
        background: var(--cream);
        color: var(--ink);
        border-color: var(--cream);
      }

      .contact .btn-organic.primary:hover {
        background: var(--specimen);
        border-color: var(--specimen);
        color: var(--cream);
      }

      /* Footer */
      footer {
        background: var(--ink);
        padding: 48px 60px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 1px solid var(--ink-light);
      }

      .footer-logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .footer-logo img {
        height: 36px;
        width: auto;
      }

      .footer-links {
        display: flex;
        gap: 40px;
      }

      .footer-links a {
        font-family: var(--font-mono);
        font-size: 10px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--ink-pale);
        text-decoration: none;
        transition: color 0.2s;
      }

      .footer-links a:hover { color: var(--cream); }

      .footer-copy {
        font-family: var(--font-mono);
        font-size: 10px;
        color: var(--ink-muted);
      }

      /* Animations */
      @keyframes draw {
        from { stroke-dashoffset: 1000; }
        to { stroke-dashoffset: 0; }
      }

      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
      }

      .animate-in {
        animation: fadeIn 0.8s ease forwards;
      }

      .delay-1 { animation-delay: 0.1s; opacity: 0; }
      .delay-2 { animation-delay: 0.2s; opacity: 0; }
      .delay-3 { animation-delay: 0.3s; opacity: 0; }
      .delay-4 { animation-delay: 0.4s; opacity: 0; }

      /* Responsive */
      @media (max-width: 1024px) {
        header { padding: 0 32px; }
        nav { display: none; }
        .hero {
          grid-template-columns: 1fr;
          padding: 140px 32px 80px;
        }
        .hero-visual { display: none; }
        .hero-annotation { display: none; }
        .stats { grid-template-columns: repeat(2, 1fr); }
        .stat:not(:last-child)::after { display: none; }
        .solutions, .products, .about, .contact { padding: 80px 32px; }
        .solutions-grid { grid-template-columns: 1fr; }
        .products-grid { grid-template-columns: 1fr; }
        .about { grid-template-columns: 1fr; gap: 60px; }
        footer { flex-direction: column; gap: 24px; text-align: center; padding: 40px 32px; }
      }
    </style>
  </head>
  <body>
    <div class="grid-bg"></div>

    <div class="concept-nav">
      <a href="/" title="Original">⌂</a>
      <a href="/concept-1" title="Soft Gradient">1</a>
      <a href="/concept-2" title="Neon Grid">2</a>
      <a href="/concept-3" title="Aurora Flow">3</a>
      <a href="/concept-4" title="Heavy Metal">4</a>
      <a href="/concept-5" title="Editorial">5</a>
      <a href="/concept-6" title="Brutalist">6</a>
      <a href="/concept-7" class="active" title="Blueprint">7</a>
      <a href="/concept-8" title="Retro">8</a>
      <a href="/concept-9" title="Swiss">9</a>
      <a href="/concept-10" title="Kinetic">10</a>
    </div>

    <header>
      <a href="/" class="logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <nav>
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
      <a href="https://store.elmlakelabs.com" class="header-cta" target="_blank">Shop Now</a>
    </header>

    <section class="hero">
      <div class="hero-content">
        <div class="hero-specimen animate-in">Specimen Collection — Precision Agriculture</div>
        <h1 class="hero-title animate-in delay-1">
          <span>Where Technology</span>
          <span>Meets the</span>
          <span class="accent">Living Land</span>
          <span class="hero-annotation">Est. 2020 — Wisconsin</span>
        </h1>
        <p class="hero-desc animate-in delay-2">
          Cultivating precision through GPS guidance, auto-steer systems, and
          farm management software—tools as refined as the crops they nurture.
        </p>
        <div class="hero-ctas animate-in delay-3">
          <a href="#products" class="btn-organic primary">
            View Collection
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a href="#about" class="btn-organic secondary">About Us</a>
        </div>
      </div>
      <div class="hero-visual animate-in delay-4">
        <div class="hero-diagram">
          <div class="diagram-label top">PRECISION</div>
          <div class="diagram-label right">GUIDANCE</div>
          <div class="diagram-label bottom">CULTIVATION</div>
          <div class="diagram-label left">HARVEST</div>
          <div class="diagram-dot n"></div>
          <div class="diagram-dot e"></div>
          <div class="diagram-dot s"></div>
          <div class="diagram-dot w"></div>
          <div class="hero-image-frame">
            <img src="/images/hero-field.jpg" alt="Agricultural specimen" class="hero-image" />
          </div>
        </div>
      </div>
    </section>

    <div class="stats">
      <div class="stat">
        <div class="stat-value">100+</div>
        <div class="stat-label">Farms Equipped</div>
      </div>
      <div class="stat">
        <div class="stat-value">±1"</div>
        <div class="stat-label">RTK Accuracy</div>
      </div>
      <div class="stat">
        <div class="stat-value">24/7</div>
        <div class="stat-label">Expert Support</div>
      </div>
      <div class="stat">
        <div class="stat-value">15%</div>
        <div class="stat-label">Fuel Savings</div>
      </div>
    </div>

    <section id="solutions" class="solutions">
      <div class="section-header">
        <div>
          <div class="section-meta">Catalogue Section I</div>
          <h2 class="section-title">Our Solutions</h2>
        </div>
        <div class="section-number">01</div>
      </div>

      <div class="solutions-grid">
        <div class="solution-card">
          <div class="solution-number">Specimen 001</div>
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <circle cx="12" cy="12" r="10"/>
              <circle cx="12" cy="12" r="3"/>
              <path d="M12 2v4M12 18v4M2 12h4M18 12h4"/>
            </svg>
          </div>
          <h3 class="solution-title">GPS Guidance</h3>
          <p class="solution-desc">Sub-inch precision positioning that reduces overlap and enables continuous field operation regardless of visibility.</p>
          <a href="#products" class="solution-link">Study specimen →</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">Specimen 002</div>
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <rect x="3" y="3" width="18" height="18" rx="2"/>
              <path d="M3 9h18M9 9v12"/>
            </svg>
          </div>
          <h3 class="solution-title">Land Leveling</h3>
          <p class="solution-desc">3D GPS-based terrain shaping for optimal water distribution and field grade perfection.</p>
          <a href="#products" class="solution-link">Study specimen →</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">Specimen 003</div>
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <h3 class="solution-title">Expert Support</h3>
          <p class="solution-desc">Dedicated agronomists and technicians who understand the living systems you cultivate.</p>
          <a href="#contact" class="solution-link">Consult experts →</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">Specimen 004</div>
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <rect x="2" y="3" width="20" height="14" rx="2"/>
              <path d="M8 21h8M12 17v4"/>
            </svg>
          </div>
          <h3 class="solution-title">Farm Software</h3>
          <p class="solution-desc">Blossom: comprehensive field documentation, seasonal tracking, and analytical insights.</p>
          <a href="#products" class="solution-link">Study specimen →</a>
        </div>
      </div>
    </section>

    <section id="products" class="products">
      <div class="section-header">
        <div>
          <div class="section-meta">Catalogue Section II</div>
          <h2 class="section-title">Featured Equipment</h2>
        </div>
        <div class="section-number">02</div>
      </div>

      <div class="products-grid">
        <div class="product-card">
          <div class="product-specimen">Sp. AT2-001</div>
          <div class="product-image-wrap">
            <img src="/images/products/at2-autosteer.png" alt="FJD AT2" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Auto-Steer System</div>
            <h3 class="product-name">FJD AT2</h3>
            <p class="product-desc">Advanced auto-steering with centimeter-level accuracy and universal compatibility.</p>
            <div class="product-footer">
              <span class="product-price">$7,199</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Acquire →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-specimen">Sp. AL01-002</div>
          <div class="product-image-wrap">
            <img src="/images/products/al01-land-leveling.png" alt="FJD AL01" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Land Leveling</div>
            <h3 class="product-name">FJD AL01 3D</h3>
            <p class="product-desc">Precision 3D land leveling for perfect field grades and water management.</p>
            <div class="product-footer">
              <span class="product-price">$6,799</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Acquire →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-specimen">Sp. V1T-003</div>
          <div class="product-image-wrap">
            <img src="/images/products/trion-v1t-rover.png" alt="FJD Trion V1t" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">RTK Rover</div>
            <h3 class="product-name">FJD Trion V1t</h3>
            <p class="product-desc">Professional RTK rover for surveying and precision field mapping.</p>
            <div class="product-footer">
              <span class="product-price">$4,249</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Acquire →</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="about" class="about">
      <div class="about-visual">
        <div class="about-frame">
          <img src="/images/hero-field.jpg" alt="Field study" class="about-image" />
          <div class="about-caption">Agricultural operations, Central Wisconsin — 2024</div>
        </div>
        <div class="about-badge">
          <div class="about-badge-value">100+</div>
          <div class="about-badge-label">Farms Equipped</div>
        </div>
      </div>
      <div class="about-content">
        <div class="section-meta">Catalogue Section III</div>
        <h2 class="section-title">Cultivating Progress</h2>
        <p class="about-desc">
          Elm Lake Labs exists at the intersection of agricultural tradition
          and technological innovation. We understand that the land is a living
          system—and we provide the precision tools to nurture it thoughtfully.
        </p>
        <div class="values-list">
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
                <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </div>
            <span class="value-text">Innovation rooted in understanding</span>
          </div>
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              </svg>
            </div>
            <span class="value-text">Support that grows with you</span>
          </div>
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
                <circle cx="12" cy="12" r="10"/>
                <path d="M12 6v6l4 2"/>
              </svg>
            </div>
            <span class="value-text">Precision across every season</span>
          </div>
        </div>
      </div>
    </section>

    <section id="contact" class="contact">
      <div class="contact-content">
        <h2 class="contact-title">Begin Your <span>Study</span></h2>
        <p class="contact-desc">
          Discover how precision agriculture can transform your cultivation practice.
        </p>
        <a href="mailto:hello@elmlakelabs.com" class="btn-organic primary">
          Make Contact
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </section>

    <footer>
      <a href="/" class="footer-logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <div class="footer-links">
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a href="https://store.elmlakelabs.com" target="_blank">Shop</a>
      </div>
      <span class="footer-copy">© 2024 Elm Lake Labs</span>
    </footer>
  </body>
</html>
