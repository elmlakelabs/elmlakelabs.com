---
/**
 * CONCEPT 9: "SWISS PRECISION"
 * Aesthetic: International Typographic Style, pure function, grid-based clarity
 * Feel: Authoritative, clean, no-nonsense Swiss design perfection
 * Typography: Suisse-style grotesque (Neue Haas Grotesk / Helvetica-esque) via Inter Tight
 * Colors: Pure white, deep black, red accent, systematic grays
 * Unique: Strong grid, asymmetric balance, typographic hierarchy, stark simplicity
 */
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Elm Lake Labs | Concept 9 - Swiss Precision</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter+Tight:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
      :root {
        --white: #FFFFFF;
        --off-white: #FAFAFA;
        --gray-100: #F5F5F5;
        --gray-200: #E5E5E5;
        --gray-300: #D4D4D4;
        --gray-400: #A3A3A3;
        --gray-500: #737373;
        --gray-600: #525252;
        --gray-700: #404040;
        --gray-800: #262626;
        --gray-900: #171717;
        --black: #0A0A0A;
        --red: #DC2626;
        --red-dark: #B91C1C;

        --font-main: 'Inter Tight', -apple-system, BlinkMacSystemFont, sans-serif;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }

      html { scroll-behavior: smooth; }

      body {
        font-family: var(--font-main);
        background: var(--white);
        color: var(--black);
        line-height: 1.5;
        font-size: 16px;
      }

      /* Grid system */
      .container {
        max-width: 1440px;
        margin: 0 auto;
        padding: 0 48px;
      }

      /* Concept switcher */
      .concept-nav {
        position: fixed;
        top: 24px;
        right: 24px;
        z-index: 1001;
        display: flex;
        gap: 2px;
        background: var(--black);
        padding: 4px;
      }
      .concept-nav a {
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 12px;
        font-weight: 600;
        text-decoration: none;
        color: var(--gray-500);
        transition: all 0.15s;
      }
      .concept-nav a:hover {
        color: var(--white);
      }
      .concept-nav a.active {
        background: var(--red);
        color: var(--white);
      }

      /* Header */
      header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        padding: 0 48px;
        height: 72px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: var(--white);
        border-bottom: 1px solid var(--gray-200);
      }

      .logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .logo img {
        height: 44px;
        width: auto;
        /* Invert for dark logo on light background */
        filter: brightness(0);
      }

      nav {
        display: flex;
        gap: 48px;
      }

      nav a {
        font-size: 14px;
        font-weight: 500;
        color: var(--gray-600);
        text-decoration: none;
        transition: color 0.15s;
      }

      nav a:hover { color: var(--black); }

      .header-cta {
        font-size: 14px;
        font-weight: 600;
        padding: 10px 24px;
        background: var(--black);
        color: var(--white);
        text-decoration: none;
        transition: all 0.15s;
      }

      .header-cta:hover {
        background: var(--red);
      }

      /* Hero */
      .hero {
        min-height: 100vh;
        padding: 120px 48px 80px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: end;
        position: relative;
      }

      .hero::before {
        content: '';
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        width: 50%;
        background: var(--gray-100);
        z-index: -1;
      }

      .hero-content {
        padding-bottom: 80px;
      }

      .hero-label {
        display: inline-block;
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--red);
        margin-bottom: 32px;
        padding-left: 32px;
        position: relative;
      }

      .hero-label::before {
        content: '';
        position: absolute;
        left: 0;
        top: 50%;
        width: 20px;
        height: 1px;
        background: var(--red);
      }

      .hero-title {
        font-size: clamp(56px, 8vw, 96px);
        font-weight: 800;
        line-height: 0.95;
        letter-spacing: -0.03em;
        color: var(--black);
        margin-bottom: 40px;
      }

      .hero-title span {
        display: block;
      }

      .hero-desc {
        font-size: 18px;
        font-weight: 400;
        color: var(--gray-600);
        max-width: 440px;
        margin-bottom: 48px;
        line-height: 1.6;
      }

      .hero-ctas {
        display: flex;
        gap: 16px;
      }

      .btn-swiss {
        font-size: 14px;
        font-weight: 600;
        padding: 16px 32px;
        text-decoration: none;
        transition: all 0.15s;
        display: inline-flex;
        align-items: center;
        gap: 12px;
      }

      .btn-swiss.primary {
        background: var(--black);
        color: var(--white);
      }

      .btn-swiss.primary:hover {
        background: var(--red);
      }

      .btn-swiss.secondary {
        background: transparent;
        color: var(--black);
        border: 1px solid var(--gray-300);
      }

      .btn-swiss.secondary:hover {
        border-color: var(--black);
      }

      .hero-visual {
        position: relative;
        padding: 80px;
        display: flex;
        flex-direction: column;
        justify-content: flex-end;
        height: 100%;
      }

      .hero-image {
        width: 100%;
        aspect-ratio: 4/3;
        object-fit: cover;
        filter: grayscale(100%);
      }

      .hero-stats {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1px;
        background: var(--gray-300);
        margin-top: 40px;
      }

      .hero-stat {
        background: var(--white);
        padding: 24px;
        text-align: center;
      }

      .hero-stat-value {
        font-size: 36px;
        font-weight: 800;
        color: var(--black);
        letter-spacing: -0.02em;
        line-height: 1;
      }

      .hero-stat-label {
        font-size: 11px;
        font-weight: 500;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--gray-500);
        margin-top: 8px;
      }

      /* Stats bar */
      .stats-bar {
        background: var(--black);
        padding: 64px 48px;
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 1px;
      }

      .stat-item {
        text-align: center;
        padding: 32px;
        position: relative;
      }

      .stat-item:not(:last-child)::after {
        content: '';
        position: absolute;
        right: 0;
        top: 50%;
        transform: translateY(-50%);
        width: 1px;
        height: 60px;
        background: var(--gray-700);
      }

      .stat-number {
        font-size: 56px;
        font-weight: 800;
        color: var(--white);
        letter-spacing: -0.03em;
        line-height: 1;
      }

      .stat-label {
        font-size: 12px;
        font-weight: 500;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--gray-400);
        margin-top: 16px;
      }

      /* Solutions */
      .solutions {
        padding: 120px 48px;
        background: var(--white);
      }

      .section-header {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: end;
        margin-bottom: 80px;
        padding-bottom: 40px;
        border-bottom: 1px solid var(--gray-200);
      }

      .section-label {
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--red);
        margin-bottom: 16px;
        display: flex;
        align-items: center;
        gap: 16px;
      }

      .section-label::before {
        content: '';
        width: 20px;
        height: 1px;
        background: var(--red);
      }

      .section-title {
        font-size: clamp(40px, 5vw, 64px);
        font-weight: 800;
        letter-spacing: -0.03em;
        color: var(--black);
        line-height: 1;
      }

      .section-desc {
        font-size: 18px;
        color: var(--gray-600);
        line-height: 1.6;
        max-width: 480px;
      }

      .solutions-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 1px;
        background: var(--gray-200);
      }

      .solution-card {
        background: var(--white);
        padding: 40px 32px;
        transition: all 0.2s;
        position: relative;
      }

      .solution-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 0;
        height: 3px;
        background: var(--red);
        transition: width 0.3s;
      }

      .solution-card:hover::before {
        width: 100%;
      }

      .solution-card:hover {
        background: var(--gray-100);
      }

      .solution-number {
        font-size: 48px;
        font-weight: 800;
        color: var(--gray-200);
        letter-spacing: -0.02em;
        line-height: 1;
        margin-bottom: 32px;
        transition: color 0.2s;
      }

      .solution-card:hover .solution-number {
        color: var(--red);
      }

      .solution-title {
        font-size: 20px;
        font-weight: 700;
        color: var(--black);
        margin-bottom: 16px;
        letter-spacing: -0.01em;
      }

      .solution-desc {
        font-size: 14px;
        color: var(--gray-500);
        line-height: 1.6;
        margin-bottom: 32px;
      }

      .solution-link {
        font-size: 12px;
        font-weight: 600;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--black);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: color 0.2s;
      }

      .solution-link:hover { color: var(--red); }

      /* Products */
      .products {
        padding: 120px 48px;
        background: var(--gray-100);
      }

      .products-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 32px;
        margin-top: 80px;
      }

      .product-card {
        background: var(--white);
        transition: all 0.2s;
      }

      .product-card:hover {
        box-shadow: 0 24px 48px rgba(0, 0, 0, 0.08);
        transform: translateY(-4px);
      }

      .product-image-wrap {
        aspect-ratio: 4/3;
        background: var(--gray-100);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 48px;
        position: relative;
      }

      .product-badge {
        position: absolute;
        top: 16px;
        left: 16px;
        font-size: 10px;
        font-weight: 600;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        padding: 6px 12px;
        background: var(--red);
        color: var(--white);
      }

      .product-image {
        max-width: 80%;
        max-height: 140px;
        object-fit: contain;
        transition: transform 0.3s;
      }

      .product-card:hover .product-image {
        transform: scale(1.05);
      }

      .product-content {
        padding: 32px;
        border-top: 1px solid var(--gray-200);
      }

      .product-category {
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--gray-500);
        margin-bottom: 8px;
      }

      .product-name {
        font-size: 24px;
        font-weight: 700;
        color: var(--black);
        margin-bottom: 12px;
        letter-spacing: -0.02em;
      }

      .product-desc {
        font-size: 14px;
        color: var(--gray-500);
        line-height: 1.6;
        margin-bottom: 24px;
      }

      .product-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 24px;
        border-top: 1px solid var(--gray-200);
      }

      .product-price {
        font-size: 28px;
        font-weight: 800;
        color: var(--black);
        letter-spacing: -0.02em;
      }

      .product-link {
        font-size: 12px;
        font-weight: 600;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--gray-500);
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 8px;
        transition: color 0.2s;
      }

      .product-link:hover { color: var(--red); }

      /* About */
      .about {
        padding: 120px 48px;
        background: var(--white);
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 120px;
        align-items: center;
      }

      .about-visual {
        position: relative;
      }

      .about-image {
        width: 100%;
        aspect-ratio: 3/4;
        object-fit: cover;
        filter: grayscale(100%);
      }

      .about-badge {
        position: absolute;
        bottom: -32px;
        right: -32px;
        background: var(--red);
        color: var(--white);
        padding: 32px 40px;
        text-align: center;
      }

      .about-badge-value {
        font-size: 48px;
        font-weight: 800;
        line-height: 1;
        letter-spacing: -0.02em;
      }

      .about-badge-label {
        font-size: 11px;
        font-weight: 500;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        margin-top: 8px;
      }

      .about-content .section-label {
        margin-bottom: 24px;
      }

      .about-content .section-title {
        margin-bottom: 32px;
      }

      .about-desc {
        font-size: 18px;
        color: var(--gray-600);
        line-height: 1.7;
        margin-bottom: 48px;
      }

      .values-list {
        display: flex;
        flex-direction: column;
        gap: 1px;
        background: var(--gray-200);
      }

      .value-item {
        display: flex;
        align-items: center;
        gap: 24px;
        padding: 24px 0;
        background: var(--white);
        transition: all 0.2s;
      }

      .value-item:hover {
        padding-left: 24px;
        background: var(--gray-100);
      }

      .value-number {
        font-size: 14px;
        font-weight: 700;
        color: var(--red);
        width: 32px;
        flex-shrink: 0;
      }

      .value-text {
        font-size: 16px;
        font-weight: 500;
        color: var(--black);
      }

      /* Contact */
      .contact {
        padding: 120px 48px;
        background: var(--black);
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: center;
      }

      .contact-content .section-label {
        color: var(--red);
      }

      .contact-title {
        font-size: clamp(40px, 5vw, 64px);
        font-weight: 800;
        letter-spacing: -0.03em;
        color: var(--white);
        line-height: 1;
        margin-bottom: 24px;
      }

      .contact-desc {
        font-size: 18px;
        color: var(--gray-400);
        max-width: 400px;
        line-height: 1.6;
      }

      .contact-cta {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: 32px;
      }

      .contact .btn-swiss.primary {
        background: var(--red);
      }

      .contact .btn-swiss.primary:hover {
        background: var(--red-dark);
      }

      .contact-info {
        display: flex;
        flex-direction: column;
        gap: 16px;
      }

      .contact-info a {
        font-size: 14px;
        color: var(--gray-400);
        text-decoration: none;
        transition: color 0.15s;
      }

      .contact-info a:hover { color: var(--white); }

      /* Footer */
      footer {
        background: var(--black);
        padding: 48px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 1px solid var(--gray-800);
      }

      .footer-logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .footer-logo img {
        height: 36px;
        width: auto;
      }

      .footer-links {
        display: flex;
        gap: 48px;
      }

      .footer-links a {
        font-size: 13px;
        font-weight: 500;
        color: var(--gray-400);
        text-decoration: none;
        transition: color 0.15s;
      }

      .footer-links a:hover { color: var(--white); }

      .footer-copy {
        font-size: 12px;
        color: var(--gray-500);
      }

      /* Animations */
      @keyframes slideUp {
        from { opacity: 0; transform: translateY(24px); }
        to { opacity: 1; transform: translateY(0); }
      }

      .animate-in {
        animation: slideUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      }

      .delay-1 { animation-delay: 0.1s; opacity: 0; }
      .delay-2 { animation-delay: 0.2s; opacity: 0; }
      .delay-3 { animation-delay: 0.3s; opacity: 0; }
      .delay-4 { animation-delay: 0.4s; opacity: 0; }

      /* Responsive */
      @media (max-width: 1024px) {
        header { padding: 0 24px; }
        nav { display: none; }
        .hero {
          grid-template-columns: 1fr;
          padding: 120px 24px 80px;
        }
        .hero::before { display: none; }
        .hero-visual { padding: 40px 0; }
        .stats-bar { grid-template-columns: repeat(2, 1fr); }
        .stat-item:not(:last-child)::after { display: none; }
        .solutions, .products, .about, .contact { padding: 80px 24px; }
        .section-header { grid-template-columns: 1fr; gap: 24px; }
        .solutions-grid { grid-template-columns: repeat(2, 1fr); }
        .products-grid { grid-template-columns: 1fr; }
        .about { grid-template-columns: 1fr; gap: 60px; }
        .contact { grid-template-columns: 1fr; }
        footer { flex-direction: column; gap: 24px; text-align: center; padding: 40px 24px; }
      }

      @media (max-width: 640px) {
        .solutions-grid { grid-template-columns: 1fr; }
        .hero-stats { grid-template-columns: 1fr; }
      }
    </style>
  </head>
  <body>
    <div class="concept-nav">
      <a href="/" title="Original">⌂</a>
      <a href="/concept-1" title="Soft Gradient">1</a>
      <a href="/concept-2" title="Neon Grid">2</a>
      <a href="/concept-3" title="Aurora Flow">3</a>
      <a href="/concept-4" title="Heavy Metal">4</a>
      <a href="/concept-5" title="Editorial">5</a>
      <a href="/concept-6" title="Brutalist">6</a>
      <a href="/concept-7" title="Blueprint">7</a>
      <a href="/concept-8" title="Retro">8</a>
      <a href="/concept-9" class="active" title="Swiss">9</a>
      <a href="/concept-10" title="Kinetic">10</a>
    </div>

    <header>
      <a href="/" class="logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <nav>
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
      <a href="https://store.elmlakelabs.com" class="header-cta" target="_blank">Shop</a>
    </header>

    <section class="hero">
      <div class="hero-content">
        <span class="hero-label animate-in">Precision Agriculture</span>
        <h1 class="hero-title animate-in delay-1">
          <span>The Future</span>
          <span>of Farming</span>
          <span>is Precise</span>
        </h1>
        <p class="hero-desc animate-in delay-2">
          GPS guidance, auto-steer systems, and farm management software
          engineered to help you unlock your full potential.
        </p>
        <div class="hero-ctas animate-in delay-3">
          <a href="#products" class="btn-swiss primary">
            Explore Products
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a href="#about" class="btn-swiss secondary">Learn More</a>
        </div>
      </div>
      <div class="hero-visual animate-in delay-4">
        <img src="/images/hero-field.jpg" alt="Agricultural field" class="hero-image" />
        <div class="hero-stats">
          <div class="hero-stat">
            <div class="hero-stat-value">±1"</div>
            <div class="hero-stat-label">RTK Accuracy</div>
          </div>
          <div class="hero-stat">
            <div class="hero-stat-value">24/7</div>
            <div class="hero-stat-label">Support</div>
          </div>
          <div class="hero-stat">
            <div class="hero-stat-value">15%</div>
            <div class="hero-stat-label">Fuel Savings</div>
          </div>
        </div>
      </div>
    </section>

    <div class="stats-bar">
      <div class="stat-item">
        <div class="stat-number">100+</div>
        <div class="stat-label">Farms Equipped</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">±1"</div>
        <div class="stat-label">RTK Accuracy</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">24/7</div>
        <div class="stat-label">Expert Support</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">15%</div>
        <div class="stat-label">Fuel Savings</div>
      </div>
    </div>

    <section id="solutions" class="solutions">
      <div class="section-header">
        <div>
          <span class="section-label">What We Do</span>
          <h2 class="section-title">Solutions</h2>
        </div>
        <p class="section-desc">
          Comprehensive precision agriculture technology designed for
          operations of every scale.
        </p>
      </div>

      <div class="solutions-grid">
        <div class="solution-card">
          <div class="solution-number">01</div>
          <h3 class="solution-title">GPS Guidance</h3>
          <p class="solution-desc">Sub-inch precision that reduces overlap and enables 24/7 operation.</p>
          <a href="#products" class="solution-link">Learn more →</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">02</div>
          <h3 class="solution-title">Land Leveling</h3>
          <p class="solution-desc">3D GPS-based leveling for perfect field grades and water efficiency.</p>
          <a href="#products" class="solution-link">Learn more →</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">03</div>
          <h3 class="solution-title">Expert Support</h3>
          <p class="solution-desc">Personalized guidance from specialists who understand your operation.</p>
          <a href="#contact" class="solution-link">Contact us →</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">04</div>
          <h3 class="solution-title">Farm Software</h3>
          <p class="solution-desc">Blossom: field records, tracking, and analytics in one platform.</p>
          <a href="#products" class="solution-link">Learn more →</a>
        </div>
      </div>
    </section>

    <section id="products" class="products">
      <div class="section-header">
        <div>
          <span class="section-label">Equipment</span>
          <h2 class="section-title">Products</h2>
        </div>
        <p class="section-desc">
          Precision technology built for real-world agricultural demands.
        </p>
      </div>

      <div class="products-grid">
        <div class="product-card">
          <div class="product-image-wrap">
            <div class="product-badge">Featured</div>
            <img src="/images/products/at2-autosteer.png" alt="FJD AT2" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Auto-Steer System</div>
            <h3 class="product-name">FJD AT2</h3>
            <p class="product-desc">Advanced auto-steering with centimeter-level accuracy.</p>
            <div class="product-footer">
              <span class="product-price">$7,199</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Shop →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrap">
            <img src="/images/products/al01-land-leveling.png" alt="FJD AL01" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Land Leveling</div>
            <h3 class="product-name">FJD AL01 3D</h3>
            <p class="product-desc">Precision 3D land leveling for perfect field grades.</p>
            <div class="product-footer">
              <span class="product-price">$6,799</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Shop →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrap">
            <img src="/images/products/trion-v1t-rover.png" alt="FJD Trion V1t" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">RTK Rover</div>
            <h3 class="product-name">FJD Trion V1t</h3>
            <p class="product-desc">Professional RTK rover for surveying and mapping.</p>
            <div class="product-footer">
              <span class="product-price">$4,249</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Shop →</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="about" class="about">
      <div class="about-visual">
        <img src="/images/hero-field.jpg" alt="Agricultural operation" class="about-image" />
        <div class="about-badge">
          <div class="about-badge-value">100+</div>
          <div class="about-badge-label">Farms Equipped</div>
        </div>
      </div>
      <div class="about-content">
        <span class="section-label">About Us</span>
        <h2 class="section-title">Built for Farmers</h2>
        <p class="about-desc">
          Elm Lake Labs is dedicated to helping farmers unlock their full potential
          through state-of-the-art precision technology. From small family farms to
          large commercial operations, we provide the tools and expertise you need.
        </p>
        <div class="values-list">
          <div class="value-item">
            <span class="value-number">01</span>
            <span class="value-text">Cutting-edge technology for every scale</span>
          </div>
          <div class="value-item">
            <span class="value-number">02</span>
            <span class="value-text">Expert guidance and 24/7 support</span>
          </div>
          <div class="value-item">
            <span class="value-number">03</span>
            <span class="value-text">Results that exceed expectations</span>
          </div>
        </div>
      </div>
    </section>

    <section id="contact" class="contact">
      <div class="contact-content">
        <span class="section-label">Get in Touch</span>
        <h2 class="contact-title">Ready to Transform Your Operation?</h2>
        <p class="contact-desc">
          Let's discuss how precision agriculture technology can work for your farm.
        </p>
      </div>
      <div class="contact-cta">
        <a href="mailto:hello@elmlakelabs.com" class="btn-swiss primary">
          Contact Us
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
        <div class="contact-info">
          <a href="mailto:hello@elmlakelabs.com">hello@elmlakelabs.com</a>
          <a href="https://store.elmlakelabs.com" target="_blank">store.elmlakelabs.com</a>
        </div>
      </div>
    </section>

    <footer>
      <a href="/" class="footer-logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <div class="footer-links">
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a href="https://store.elmlakelabs.com" target="_blank">Shop</a>
      </div>
      <span class="footer-copy">© 2024 Elm Lake Labs</span>
    </footer>
  </body>
</html>
