---
/**
 * CONCEPT 8: "RETRO-FUTURISM"
 * Aesthetic: 1970s NASA meets farming, space-age agriculture
 * Feel: Optimistic futurism, analog warmth, mission control vibes
 * Typography: Futuristic condensed (Orbitron) + warm retro body (DM Sans)
 * Colors: Burnt orange, cream, olive green, space black
 * Unique: Circular gauges, mission patches, vintage space graphics
 */
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Elm Lake Labs | Concept 8 - Retro-Futurism</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700;800;900&family=DM+Sans:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">
    <style>
      :root {
        --cream: #FDF6E3;
        --cream-dark: #EDE4D0;
        --orange: #D65D0E;
        --orange-light: #FE8019;
        --olive: #79740E;
        --olive-dark: #5D5D0E;
        --space-black: #1D1F21;
        --space-gray: #282A2E;
        --gray: #928374;
        --white: #FBFBFB;

        --font-display: 'Orbitron', sans-serif;
        --font-body: 'DM Sans', sans-serif;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }

      html { scroll-behavior: smooth; }

      body {
        font-family: var(--font-body);
        background: var(--space-black);
        color: var(--cream);
        line-height: 1.6;
      }

      /* Scanline effect */
      body::before {
        content: '';
        position: fixed;
        inset: 0;
        background: repeating-linear-gradient(
          0deg,
          transparent,
          transparent 2px,
          rgba(0, 0, 0, 0.03) 2px,
          rgba(0, 0, 0, 0.03) 4px
        );
        pointer-events: none;
        z-index: 1000;
      }

      /* Concept switcher */
      .concept-nav {
        position: fixed;
        top: 20px;
        right: 20px;
        z-index: 1001;
        display: flex;
        gap: 4px;
        padding: 8px;
        background: var(--space-gray);
        border: 2px solid var(--orange);
        border-radius: 8px;
      }
      .concept-nav a {
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: var(--font-display);
        font-size: 11px;
        text-decoration: none;
        color: var(--gray);
        border-radius: 4px;
        transition: all 0.2s;
      }
      .concept-nav a:hover {
        background: var(--space-black);
        color: var(--cream);
      }
      .concept-nav a.active {
        background: var(--orange);
        color: var(--space-black);
      }

      /* Header */
      header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        padding: 0 48px;
        height: 80px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: var(--space-black);
        border-bottom: 2px solid var(--space-gray);
      }

      .logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .logo img {
        height: 52px;
        width: auto;
      }

      nav {
        display: flex;
        gap: 40px;
      }

      nav a {
        font-family: var(--font-display);
        font-size: 11px;
        font-weight: 500;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--gray);
        text-decoration: none;
        transition: color 0.2s;
      }

      nav a:hover { color: var(--orange); }

      .header-cta {
        font-family: var(--font-display);
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        padding: 14px 28px;
        background: var(--orange);
        color: var(--space-black);
        text-decoration: none;
        border-radius: 4px;
        transition: all 0.2s;
      }

      .header-cta:hover {
        background: var(--orange-light);
        box-shadow: 0 0 20px rgba(214, 93, 14, 0.5);
      }

      /* Hero */
      .hero {
        min-height: 100vh;
        padding: 160px 48px 100px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: center;
        position: relative;
        overflow: hidden;
      }

      .hero::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 800px;
        height: 800px;
        background: radial-gradient(circle, rgba(214, 93, 14, 0.1) 0%, transparent 70%);
        transform: translate(-50%, -50%);
        pointer-events: none;
      }

      .hero-content {
        position: relative;
        z-index: 1;
      }

      .hero-mission {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        font-family: var(--font-display);
        font-size: 10px;
        font-weight: 500;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--olive);
        margin-bottom: 24px;
        padding: 8px 16px;
        border: 1px solid var(--olive);
        border-radius: 100px;
      }

      .hero-mission .status {
        width: 8px;
        height: 8px;
        background: var(--olive);
        border-radius: 50%;
        animation: pulse 2s infinite;
      }

      @keyframes pulse {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.5; transform: scale(1.2); }
      }

      .hero-title {
        font-family: var(--font-display);
        font-size: clamp(48px, 8vw, 96px);
        font-weight: 900;
        line-height: 1;
        letter-spacing: -0.02em;
        text-transform: uppercase;
        color: var(--cream);
        margin-bottom: 32px;
      }

      .hero-title span {
        display: block;
      }

      .hero-title .accent {
        color: var(--orange);
      }

      .hero-desc {
        font-size: 18px;
        color: var(--gray);
        max-width: 480px;
        margin-bottom: 48px;
        line-height: 1.7;
      }

      .hero-ctas {
        display: flex;
        gap: 16px;
      }

      .btn-retro {
        font-family: var(--font-display);
        font-size: 11px;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        padding: 18px 36px;
        text-decoration: none;
        border-radius: 4px;
        transition: all 0.2s;
        display: inline-flex;
        align-items: center;
        gap: 12px;
      }

      .btn-retro.primary {
        background: var(--orange);
        color: var(--space-black);
      }

      .btn-retro.primary:hover {
        background: var(--orange-light);
        box-shadow: 0 0 30px rgba(214, 93, 14, 0.4);
      }

      .btn-retro.secondary {
        background: transparent;
        color: var(--cream);
        border: 2px solid var(--space-gray);
      }

      .btn-retro.secondary:hover {
        border-color: var(--cream);
      }

      .hero-visual {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .mission-patch {
        width: 400px;
        height: 400px;
        border-radius: 50%;
        background: var(--space-gray);
        border: 8px solid var(--orange);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        position: relative;
        box-shadow: 0 0 60px rgba(214, 93, 14, 0.2);
      }

      .mission-patch::before {
        content: 'PRECISION AGRICULTURE';
        position: absolute;
        top: 30px;
        font-family: var(--font-display);
        font-size: 12px;
        font-weight: 700;
        letter-spacing: 0.3em;
        color: var(--orange);
      }

      .mission-patch::after {
        content: 'EST. 2020 • WISCONSIN';
        position: absolute;
        bottom: 30px;
        font-family: var(--font-display);
        font-size: 10px;
        font-weight: 500;
        letter-spacing: 0.2em;
        color: var(--gray);
      }

      .patch-content {
        text-align: center;
      }

      .patch-icon {
        width: 120px;
        height: 120px;
        margin-bottom: 16px;
      }

      .patch-icon svg {
        width: 100%;
        height: 100%;
        stroke: var(--cream);
      }

      .patch-title {
        font-family: var(--font-display);
        font-size: 32px;
        font-weight: 900;
        letter-spacing: 0.1em;
        color: var(--cream);
        text-transform: uppercase;
      }

      .patch-subtitle {
        font-family: var(--font-display);
        font-size: 12px;
        font-weight: 500;
        letter-spacing: 0.2em;
        color: var(--olive);
        margin-top: 4px;
      }

      /* Gauges */
      .gauges {
        position: absolute;
        right: -60px;
        top: 50%;
        transform: translateY(-50%);
        display: flex;
        flex-direction: column;
        gap: 20px;
      }

      .gauge {
        width: 100px;
        height: 100px;
        background: var(--space-gray);
        border-radius: 50%;
        border: 3px solid var(--orange);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }

      .gauge-value {
        font-family: var(--font-display);
        font-size: 24px;
        font-weight: 700;
        color: var(--orange);
        line-height: 1;
      }

      .gauge-label {
        font-family: var(--font-display);
        font-size: 8px;
        letter-spacing: 0.1em;
        color: var(--gray);
        margin-top: 4px;
        text-transform: uppercase;
      }

      /* Stats */
      .stats {
        background: var(--space-gray);
        padding: 48px;
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 48px;
        border-top: 2px solid var(--orange);
        border-bottom: 2px solid var(--orange);
      }

      .stat {
        text-align: center;
        position: relative;
      }

      .stat::before {
        content: '';
        position: absolute;
        top: -16px;
        left: 50%;
        transform: translateX(-50%);
        width: 40px;
        height: 4px;
        background: var(--orange);
        border-radius: 2px;
      }

      .stat-value {
        font-family: var(--font-display);
        font-size: 48px;
        font-weight: 900;
        color: var(--cream);
        line-height: 1;
      }

      .stat-label {
        font-family: var(--font-display);
        font-size: 10px;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--gray);
        margin-top: 12px;
      }

      /* Solutions */
      .solutions {
        padding: 120px 48px;
        background: var(--space-black);
      }

      .section-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-end;
        margin-bottom: 80px;
        padding-bottom: 32px;
        border-bottom: 2px solid var(--space-gray);
      }

      .section-meta {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        font-family: var(--font-display);
        font-size: 10px;
        font-weight: 500;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--olive);
        margin-bottom: 16px;
      }

      .section-meta::before {
        content: '//';
        color: var(--orange);
      }

      .section-title {
        font-family: var(--font-display);
        font-size: clamp(36px, 6vw, 64px);
        font-weight: 900;
        letter-spacing: -0.01em;
        text-transform: uppercase;
        color: var(--cream);
      }

      .section-number {
        font-family: var(--font-display);
        font-size: 96px;
        font-weight: 900;
        color: var(--space-gray);
        line-height: 1;
      }

      .solutions-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 24px;
      }

      .solution-card {
        background: var(--space-gray);
        padding: 32px;
        border-radius: 8px;
        border: 2px solid transparent;
        transition: all 0.3s;
        position: relative;
        overflow: hidden;
      }

      .solution-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: var(--orange);
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.3s;
      }

      .solution-card:hover {
        border-color: var(--orange);
      }

      .solution-card:hover::before {
        transform: scaleX(1);
      }

      .solution-number {
        font-family: var(--font-display);
        font-size: 48px;
        font-weight: 900;
        color: var(--space-black);
        -webkit-text-stroke: 1px var(--gray);
        margin-bottom: 24px;
      }

      .solution-card:hover .solution-number {
        color: var(--orange);
        -webkit-text-stroke: none;
      }

      .solution-icon {
        width: 56px;
        height: 56px;
        background: var(--space-black);
        border-radius: 50%;
        border: 2px solid var(--gray);
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 24px;
        transition: all 0.3s;
      }

      .solution-card:hover .solution-icon {
        border-color: var(--orange);
        background: var(--orange);
      }

      .solution-icon svg {
        width: 28px;
        height: 28px;
        stroke: var(--gray);
        transition: stroke 0.3s;
      }

      .solution-card:hover .solution-icon svg {
        stroke: var(--space-black);
      }

      .solution-title {
        font-family: var(--font-display);
        font-size: 18px;
        font-weight: 700;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--cream);
        margin-bottom: 16px;
      }

      .solution-desc {
        font-size: 14px;
        color: var(--gray);
        line-height: 1.6;
        margin-bottom: 24px;
      }

      .solution-link {
        font-family: var(--font-display);
        font-size: 10px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--orange);
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: gap 0.2s;
      }

      .solution-link:hover { gap: 12px; }

      /* Products */
      .products {
        padding: 120px 48px;
        background: var(--cream);
        color: var(--space-black);
      }

      .products .section-title { color: var(--space-black); }
      .products .section-meta { color: var(--olive-dark); }
      .products .section-number { color: var(--cream-dark); }

      .products-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 32px;
        margin-top: 80px;
      }

      .product-card {
        background: var(--white);
        border-radius: 12px;
        overflow: hidden;
        border: 2px solid var(--cream-dark);
        transition: all 0.3s;
      }

      .product-card:hover {
        border-color: var(--orange);
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
        transform: translateY(-8px);
      }

      .product-image-wrap {
        aspect-ratio: 4/3;
        background: var(--cream);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 40px;
        position: relative;
      }

      .product-badge {
        position: absolute;
        top: 16px;
        left: 16px;
        font-family: var(--font-display);
        font-size: 9px;
        font-weight: 700;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        padding: 6px 12px;
        background: var(--orange);
        color: var(--white);
        border-radius: 4px;
      }

      .product-image {
        max-width: 80%;
        max-height: 140px;
        object-fit: contain;
        transition: transform 0.3s;
      }

      .product-card:hover .product-image {
        transform: scale(1.1);
      }

      .product-content {
        padding: 28px;
      }

      .product-category {
        font-family: var(--font-display);
        font-size: 10px;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--olive-dark);
        margin-bottom: 8px;
      }

      .product-name {
        font-family: var(--font-display);
        font-size: 24px;
        font-weight: 800;
        text-transform: uppercase;
        color: var(--space-black);
        margin-bottom: 12px;
      }

      .product-desc {
        font-size: 14px;
        color: var(--gray);
        line-height: 1.6;
        margin-bottom: 24px;
      }

      .product-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 20px;
        border-top: 2px solid var(--cream);
      }

      .product-price {
        font-family: var(--font-display);
        font-size: 28px;
        font-weight: 900;
        color: var(--orange);
      }

      .product-link {
        font-family: var(--font-display);
        font-size: 10px;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--gray);
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 6px;
        transition: color 0.2s;
      }

      .product-link:hover { color: var(--orange); }

      /* About */
      .about {
        padding: 120px 48px;
        background: var(--space-black);
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 100px;
        align-items: center;
      }

      .about-visual {
        position: relative;
      }

      .about-image-frame {
        position: relative;
        border-radius: 50%;
        overflow: hidden;
        border: 8px solid var(--orange);
        aspect-ratio: 1;
        max-width: 500px;
      }

      .about-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      .about-badge {
        position: absolute;
        bottom: 20px;
        right: -40px;
        background: var(--orange);
        color: var(--space-black);
        padding: 24px 32px;
        border-radius: 8px;
        text-align: center;
      }

      .about-badge-value {
        font-family: var(--font-display);
        font-size: 40px;
        font-weight: 900;
        line-height: 1;
      }

      .about-badge-label {
        font-family: var(--font-display);
        font-size: 10px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        margin-top: 4px;
      }

      .about-content .section-title {
        margin-bottom: 24px;
      }

      .about-desc {
        font-size: 18px;
        color: var(--gray);
        line-height: 1.8;
        margin-bottom: 40px;
      }

      .values-grid {
        display: flex;
        flex-direction: column;
        gap: 16px;
      }

      .value-item {
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 20px 24px;
        background: var(--space-gray);
        border-radius: 8px;
        border-left: 4px solid var(--orange);
        transition: all 0.2s;
      }

      .value-item:hover {
        background: rgba(214, 93, 14, 0.1);
      }

      .value-icon {
        width: 48px;
        height: 48px;
        background: var(--space-black);
        border-radius: 50%;
        border: 2px solid var(--gray);
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        transition: all 0.2s;
      }

      .value-item:hover .value-icon {
        border-color: var(--orange);
        background: var(--orange);
      }

      .value-icon svg {
        width: 24px;
        height: 24px;
        stroke: var(--gray);
        transition: stroke 0.2s;
      }

      .value-item:hover .value-icon svg {
        stroke: var(--space-black);
      }

      .value-text {
        font-family: var(--font-display);
        font-size: 14px;
        font-weight: 600;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        color: var(--cream);
      }

      /* Contact */
      .contact {
        padding: 120px 48px;
        background: var(--orange);
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .contact::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 600px;
        height: 600px;
        background: radial-gradient(circle, rgba(0,0,0,0.1) 0%, transparent 70%);
        transform: translate(-50%, -50%);
        border-radius: 50%;
      }

      .contact-content {
        position: relative;
        z-index: 1;
      }

      .contact-title {
        font-family: var(--font-display);
        font-size: clamp(36px, 7vw, 80px);
        font-weight: 900;
        text-transform: uppercase;
        color: var(--space-black);
        margin-bottom: 20px;
      }

      .contact-desc {
        font-size: 18px;
        color: var(--space-gray);
        max-width: 480px;
        margin: 0 auto 40px;
      }

      .contact .btn-retro.primary {
        background: var(--space-black);
        color: var(--cream);
      }

      .contact .btn-retro.primary:hover {
        box-shadow: 0 0 40px rgba(0, 0, 0, 0.3);
      }

      /* Footer */
      footer {
        background: var(--space-black);
        padding: 48px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 2px solid var(--space-gray);
      }

      .footer-logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .footer-logo img {
        height: 36px;
        width: auto;
      }

      .footer-links {
        display: flex;
        gap: 40px;
      }

      .footer-links a {
        font-family: var(--font-display);
        font-size: 10px;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--gray);
        text-decoration: none;
        transition: color 0.2s;
      }

      .footer-links a:hover { color: var(--orange); }

      .footer-copy {
        font-family: var(--font-display);
        font-size: 10px;
        color: var(--gray);
      }

      /* Animations */
      @keyframes slideUp {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
      }

      .animate-in {
        animation: slideUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      }

      .delay-1 { animation-delay: 0.1s; opacity: 0; }
      .delay-2 { animation-delay: 0.2s; opacity: 0; }
      .delay-3 { animation-delay: 0.3s; opacity: 0; }
      .delay-4 { animation-delay: 0.4s; opacity: 0; }

      /* Responsive */
      @media (max-width: 1024px) {
        header { padding: 0 24px; }
        nav { display: none; }
        .hero {
          grid-template-columns: 1fr;
          padding: 140px 24px 80px;
        }
        .hero-visual { display: none; }
        .stats { grid-template-columns: repeat(2, 1fr); }
        .solutions, .products, .about, .contact { padding: 80px 24px; }
        .solutions-grid { grid-template-columns: repeat(2, 1fr); }
        .products-grid { grid-template-columns: 1fr; }
        .about { grid-template-columns: 1fr; gap: 60px; }
        footer { flex-direction: column; gap: 24px; text-align: center; padding: 40px 24px; }
      }

      @media (max-width: 640px) {
        .solutions-grid { grid-template-columns: 1fr; }
      }
    </style>
  </head>
  <body>
    <div class="concept-nav">
      <a href="/" title="Original">⌂</a>
      <a href="/concept-1" title="Soft Gradient">1</a>
      <a href="/concept-2" title="Neon Grid">2</a>
      <a href="/concept-3" title="Aurora Flow">3</a>
      <a href="/concept-4" title="Heavy Metal">4</a>
      <a href="/concept-5" title="Editorial">5</a>
      <a href="/concept-6" title="Brutalist">6</a>
      <a href="/concept-7" title="Blueprint">7</a>
      <a href="/concept-8" class="active" title="Retro">8</a>
      <a href="/concept-9" title="Swiss">9</a>
      <a href="/concept-10" title="Kinetic">10</a>
    </div>

    <header>
      <a href="/" class="logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <nav>
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
      <a href="https://store.elmlakelabs.com" class="header-cta" target="_blank">Shop Now</a>
    </header>

    <section class="hero">
      <div class="hero-content">
        <div class="hero-mission animate-in">
          <span class="status"></span>
          Mission Active — Field Operations
        </div>
        <h1 class="hero-title animate-in delay-1">
          <span>Precision</span>
          <span>Agriculture</span>
          <span class="accent">Command</span>
        </h1>
        <p class="hero-desc animate-in delay-2">
          State-of-the-art GPS guidance systems and farm management software
          engineered for mission-critical agricultural operations.
        </p>
        <div class="hero-ctas animate-in delay-3">
          <a href="#products" class="btn-retro primary">
            View Systems
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a href="#about" class="btn-retro secondary">Learn More</a>
        </div>
      </div>
      <div class="hero-visual animate-in delay-4">
        <div class="mission-patch">
          <div class="patch-content">
            <div class="patch-icon">
              <svg viewBox="0 0 100 100" fill="none" stroke-width="2">
                <circle cx="50" cy="50" r="40"/>
                <circle cx="50" cy="50" r="10"/>
                <path d="M50 10v20M50 70v20M10 50h20M70 50h20"/>
                <path d="M25 25l14 14M61 61l14 14M25 75l14-14M61 39l14-14"/>
              </svg>
            </div>
            <div class="patch-title">ELM LAKE</div>
            <div class="patch-subtitle">Labs Division</div>
          </div>
        </div>
        <div class="gauges">
          <div class="gauge">
            <div class="gauge-value">±1"</div>
            <div class="gauge-label">Accuracy</div>
          </div>
          <div class="gauge">
            <div class="gauge-value">24/7</div>
            <div class="gauge-label">Support</div>
          </div>
        </div>
      </div>
    </section>

    <div class="stats">
      <div class="stat">
        <div class="stat-value">100+</div>
        <div class="stat-label">Farms Equipped</div>
      </div>
      <div class="stat">
        <div class="stat-value">±1"</div>
        <div class="stat-label">RTK Accuracy</div>
      </div>
      <div class="stat">
        <div class="stat-value">24/7</div>
        <div class="stat-label">Expert Support</div>
      </div>
      <div class="stat">
        <div class="stat-value">15%</div>
        <div class="stat-label">Fuel Savings</div>
      </div>
    </div>

    <section id="solutions" class="solutions">
      <div class="section-header">
        <div>
          <div class="section-meta">Mission Parameters</div>
          <h2 class="section-title">Systems</h2>
        </div>
        <div class="section-number">01</div>
      </div>

      <div class="solutions-grid">
        <div class="solution-card">
          <div class="solution-number">01</div>
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <circle cx="12" cy="12" r="10"/>
              <circle cx="12" cy="12" r="3"/>
              <path d="M12 2v4M12 18v4M2 12h4M18 12h4"/>
            </svg>
          </div>
          <h3 class="solution-title">GPS Guidance</h3>
          <p class="solution-desc">Sub-inch precision navigation for optimal field coverage and 24/7 operation.</p>
          <a href="#products" class="solution-link">Engage →</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">02</div>
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <rect x="3" y="3" width="18" height="18" rx="2"/>
              <path d="M3 9h18M9 9v12"/>
            </svg>
          </div>
          <h3 class="solution-title">Land Leveling</h3>
          <p class="solution-desc">3D GPS-based terrain management for perfect grade control.</p>
          <a href="#products" class="solution-link">Engage →</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">03</div>
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <h3 class="solution-title">Mission Support</h3>
          <p class="solution-desc">Dedicated specialists standing by for field operations assistance.</p>
          <a href="#contact" class="solution-link">Contact →</a>
        </div>
        <div class="solution-card">
          <div class="solution-number">04</div>
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5">
              <rect x="2" y="3" width="20" height="14" rx="2"/>
              <path d="M8 21h8M12 17v4"/>
            </svg>
          </div>
          <h3 class="solution-title">Command Software</h3>
          <p class="solution-desc">Blossom: centralized field data, tracking, and analytics platform.</p>
          <a href="#products" class="solution-link">Engage →</a>
        </div>
      </div>
    </section>

    <section id="products" class="products">
      <div class="section-header">
        <div>
          <div class="section-meta">Equipment Manifest</div>
          <h2 class="section-title">Hardware</h2>
        </div>
        <div class="section-number">02</div>
      </div>

      <div class="products-grid">
        <div class="product-card">
          <div class="product-image-wrap">
            <div class="product-badge">Primary</div>
            <img src="/images/products/at2-autosteer.png" alt="FJD AT2" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Auto-Steer System</div>
            <h3 class="product-name">FJD AT2</h3>
            <p class="product-desc">Advanced auto-steering with centimeter-level accuracy for any operation.</p>
            <div class="product-footer">
              <span class="product-price">$7,199</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Acquire →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrap">
            <img src="/images/products/al01-land-leveling.png" alt="FJD AL01" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Land Leveling</div>
            <h3 class="product-name">FJD AL01 3D</h3>
            <p class="product-desc">Precision 3D leveling system for optimal field grading.</p>
            <div class="product-footer">
              <span class="product-price">$6,799</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Acquire →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrap">
            <img src="/images/products/trion-v1t-rover.png" alt="FJD Trion V1t" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">RTK Rover</div>
            <h3 class="product-name">FJD Trion V1t</h3>
            <p class="product-desc">Professional RTK rover for surveying and mapping missions.</p>
            <div class="product-footer">
              <span class="product-price">$4,249</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Acquire →</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="about" class="about">
      <div class="about-visual">
        <div class="about-image-frame">
          <img src="/images/hero-field.jpg" alt="Mission operations" class="about-image" />
        </div>
        <div class="about-badge">
          <div class="about-badge-value">100+</div>
          <div class="about-badge-label">Missions Complete</div>
        </div>
      </div>
      <div class="about-content">
        <div class="section-meta">Mission Briefing</div>
        <h2 class="section-title">Our Purpose</h2>
        <p class="about-desc">
          Elm Lake Labs operates at the frontier of agricultural technology.
          We equip farmers with mission-critical precision systems that deliver
          results—season after season, field after field.
        </p>
        <div class="values-grid">
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </div>
            <span class="value-text">Advanced Technology Systems</span>
          </div>
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              </svg>
            </div>
            <span class="value-text">Mission-Critical Reliability</span>
          </div>
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <path d="M12 6v6l4 2"/>
              </svg>
            </div>
            <span class="value-text">24/7 Ground Control Support</span>
          </div>
        </div>
      </div>
    </section>

    <section id="contact" class="contact">
      <div class="contact-content">
        <h2 class="contact-title">Begin Your Mission</h2>
        <p class="contact-desc">
          Connect with our team to discuss how precision agriculture can transform your operation.
        </p>
        <a href="mailto:hello@elmlakelabs.com" class="btn-retro primary">
          Initiate Contact
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </section>

    <footer>
      <a href="/" class="footer-logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <div class="footer-links">
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a href="https://store.elmlakelabs.com" target="_blank">Shop</a>
      </div>
      <span class="footer-copy">© 2024 Elm Lake Labs</span>
    </footer>
  </body>
</html>
