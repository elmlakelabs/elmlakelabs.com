---
/**
 * CONCEPT 2: "DARK TECH"
 * Aesthetic: Dark theme that lets the logo's gradient colors pop
 * Feel: Premium tech, sophisticated, the logo is the color source
 * Typography: Modern sans with good weight
 * Colors: Deep dark backgrounds with logo gradient accents
 * The molecular logo becomes a design motif throughout
 */
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Elm Lake Labs | Concept 2 - Dark Tech</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
      :root {
        --bg-dark: #09090B;
        --bg-card: #18181B;
        --bg-elevated: #27272A;
        --border: rgba(255,255,255,0.08);
        --border-hover: rgba(255,255,255,0.15);
        --text: #FAFAFA;
        --text-muted: #A1A1AA;
        --text-dim: #71717A;

        /* Logo gradient colors */
        --magenta: #E91E8C;
        --purple: #9333EA;
        --blue: #0EA5E9;
        --cyan: #06B6D4;

        --gradient: linear-gradient(135deg, var(--magenta), var(--purple), var(--blue));
        --font: 'Inter', system-ui, sans-serif;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }
      html { scroll-behavior: smooth; }

      body {
        font-family: var(--font);
        background: var(--bg-dark);
        color: var(--text);
        line-height: 1.6;
      }

      /* Gradient orbs background */
      .bg-orbs {
        position: fixed;
        inset: 0;
        pointer-events: none;
        z-index: 0;
        overflow: hidden;
      }

      .orb {
        position: absolute;
        border-radius: 50%;
        filter: blur(120px);
        opacity: 0.15;
      }

      .orb-1 {
        width: 600px;
        height: 600px;
        background: var(--magenta);
        top: -200px;
        left: -200px;
      }

      .orb-2 {
        width: 500px;
        height: 500px;
        background: var(--purple);
        top: 30%;
        right: -150px;
      }

      .orb-3 {
        width: 400px;
        height: 400px;
        background: var(--blue);
        bottom: -100px;
        left: 30%;
      }

      /* Concept switcher */
      .concept-nav {
        position: fixed;
        top: 20px;
        right: 20px;
        z-index: 1000;
        display: flex;
        gap: 6px;
        padding: 8px;
        background: var(--bg-card);
        border: 1px solid var(--border);
        border-radius: 12px;
      }
      .concept-nav a {
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 8px;
        font-size: 13px;
        font-weight: 600;
        text-decoration: none;
        color: var(--text-muted);
        transition: all 0.2s;
      }
      .concept-nav a:hover { background: var(--bg-elevated); color: var(--text); }
      .concept-nav a.active {
        background: var(--gradient);
        color: white;
      }

      /* Header */
      header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        padding: 16px 48px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: rgba(9, 9, 11, 0.8);
        backdrop-filter: blur(20px);
        border-bottom: 1px solid var(--border);
      }

      .logo img {
        height: 44px;
        width: auto;
      }

      nav {
        display: flex;
        gap: 32px;
      }

      nav a {
        font-size: 14px;
        font-weight: 500;
        color: var(--text-muted);
        text-decoration: none;
        transition: color 0.2s;
      }

      nav a:hover { color: var(--text); }

      .header-cta {
        padding: 12px 24px;
        background: var(--gradient);
        color: white;
        font-size: 14px;
        font-weight: 600;
        text-decoration: none;
        border-radius: 10px;
        transition: all 0.3s;
      }

      .header-cta:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 30px rgba(147, 51, 234, 0.4);
      }

      /* Hero */
      .hero {
        min-height: 100vh;
        display: flex;
        align-items: center;
        padding: 120px 80px 80px;
        position: relative;
      }

      .hero-content {
        max-width: 640px;
        position: relative;
        z-index: 1;
      }

      .hero-badge {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        padding: 10px 18px;
        background: var(--bg-card);
        border: 1px solid var(--border);
        border-radius: 100px;
        font-size: 13px;
        font-weight: 500;
        color: var(--text);
        margin-bottom: 28px;
      }

      .hero-badge .dot {
        width: 8px;
        height: 8px;
        background: var(--gradient);
        border-radius: 50%;
        animation: pulse 2s ease infinite;
      }

      @keyframes pulse {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.7; transform: scale(1.2); }
      }

      .hero-title {
        font-size: clamp(52px, 7vw, 80px);
        font-weight: 800;
        line-height: 1.05;
        color: var(--text);
        margin-bottom: 24px;
        letter-spacing: -0.03em;
      }

      .hero-title .gradient {
        background: var(--gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .hero-desc {
        font-size: 18px;
        color: var(--text-muted);
        line-height: 1.8;
        margin-bottom: 40px;
      }

      .hero-cta-group {
        display: flex;
        gap: 16px;
        margin-bottom: 60px;
      }

      .btn-primary {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        padding: 16px 32px;
        background: var(--gradient);
        color: white;
        font-size: 15px;
        font-weight: 600;
        text-decoration: none;
        border-radius: 12px;
        transition: all 0.3s;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 40px rgba(147, 51, 234, 0.4);
      }

      .btn-secondary {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        padding: 16px 32px;
        background: var(--bg-card);
        color: var(--text);
        font-size: 15px;
        font-weight: 600;
        text-decoration: none;
        border-radius: 12px;
        border: 1px solid var(--border);
        transition: all 0.3s;
      }

      .btn-secondary:hover {
        border-color: var(--border-hover);
        background: var(--bg-elevated);
      }

      .hero-stats {
        display: flex;
        gap: 48px;
        padding-top: 32px;
        border-top: 1px solid var(--border);
      }

      .hero-stat-value {
        font-size: 32px;
        font-weight: 800;
        background: var(--gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .hero-stat-label {
        font-size: 13px;
        color: var(--text-dim);
        margin-top: 4px;
      }

      .hero-visual {
        position: absolute;
        right: 80px;
        top: 50%;
        transform: translateY(-50%);
        width: 42%;
        max-width: 500px;
      }

      .hero-image {
        width: 100%;
        border-radius: 20px;
        border: 1px solid var(--border);
      }

      .hero-card {
        position: absolute;
        background: var(--bg-card);
        border: 1px solid var(--border);
        padding: 20px 24px;
        border-radius: 16px;
        backdrop-filter: blur(10px);
      }

      .hero-card.top { top: -20px; left: -60px; }
      .hero-card.bottom { bottom: 40px; right: -60px; }

      .hero-card-icon {
        width: 44px;
        height: 44px;
        background: var(--gradient);
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 12px;
      }

      .hero-card-icon svg {
        width: 22px;
        height: 22px;
        stroke: white;
      }

      .hero-card-value {
        font-size: 24px;
        font-weight: 800;
        color: var(--text);
      }

      .hero-card-label {
        font-size: 12px;
        color: var(--text-dim);
      }

      /* Solutions */
      .solutions {
        padding: 100px 80px;
        position: relative;
        border-top: 1px solid var(--border);
      }

      .section-header {
        text-align: center;
        margin-bottom: 60px;
      }

      .section-badge {
        display: inline-flex;
        padding: 8px 16px;
        background: var(--bg-card);
        border: 1px solid var(--border);
        border-radius: 100px;
        font-size: 12px;
        font-weight: 600;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 16px;
      }

      .section-title {
        font-size: clamp(36px, 5vw, 52px);
        font-weight: 800;
        letter-spacing: -0.02em;
      }

      .solutions-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 20px;
      }

      .solution-card {
        padding: 32px;
        background: var(--bg-card);
        border: 1px solid var(--border);
        border-radius: 20px;
        transition: all 0.3s;
        position: relative;
        overflow: hidden;
      }

      .solution-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 2px;
        background: var(--gradient);
        opacity: 0;
        transition: opacity 0.3s;
      }

      .solution-card:hover {
        border-color: var(--border-hover);
        transform: translateY(-6px);
      }

      .solution-card:hover::before {
        opacity: 1;
      }

      .solution-icon {
        width: 52px;
        height: 52px;
        background: var(--bg-elevated);
        border-radius: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 20px;
      }

      .solution-icon svg {
        width: 26px;
        height: 26px;
        stroke: url(#icon-gradient);
      }

      .solution-title {
        font-size: 18px;
        font-weight: 700;
        margin-bottom: 10px;
      }

      .solution-desc {
        font-size: 14px;
        color: var(--text-muted);
        line-height: 1.7;
        margin-bottom: 20px;
      }

      .solution-link {
        font-size: 14px;
        font-weight: 600;
        background: var(--gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 6px;
      }

      /* Products */
      .products {
        padding: 100px 80px;
        background: var(--bg-card);
        border-top: 1px solid var(--border);
        border-bottom: 1px solid var(--border);
      }

      .products-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 24px;
        margin-top: 60px;
      }

      .product-card {
        background: var(--bg-dark);
        border: 1px solid var(--border);
        border-radius: 20px;
        overflow: hidden;
        transition: all 0.3s;
      }

      .product-card:hover {
        border-color: var(--border-hover);
        transform: translateY(-8px);
        box-shadow: 0 20px 60px rgba(0,0,0,0.4);
      }

      .product-image-wrap {
        background: linear-gradient(135deg, rgba(233, 30, 140, 0.08), rgba(14, 165, 233, 0.08));
        padding: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-bottom: 1px solid var(--border);
      }

      .product-image {
        width: 100%;
        height: 180px;
        object-fit: contain;
        transition: transform 0.3s;
      }

      .product-card:hover .product-image {
        transform: scale(1.05);
      }

      .product-content {
        padding: 28px;
      }

      .product-category {
        font-size: 11px;
        font-weight: 600;
        background: var(--gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 8px;
      }

      .product-name {
        font-size: 22px;
        font-weight: 700;
        margin-bottom: 8px;
      }

      .product-desc {
        font-size: 14px;
        color: var(--text-muted);
        line-height: 1.6;
        margin-bottom: 24px;
      }

      .product-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 20px;
        border-top: 1px solid var(--border);
      }

      .product-price {
        font-size: 24px;
        font-weight: 800;
        background: var(--gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .product-link {
        font-size: 14px;
        font-weight: 600;
        color: var(--text);
        text-decoration: none;
        display: flex;
        align-items: center;
        gap: 6px;
        transition: color 0.2s;
      }

      .product-link:hover { color: var(--magenta); }

      /* About */
      .about {
        padding: 100px 80px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: center;
      }

      .about-image-wrap {
        position: relative;
      }

      .about-image {
        width: 100%;
        border-radius: 20px;
        border: 1px solid var(--border);
      }

      .about-badge {
        position: absolute;
        bottom: -24px;
        right: -24px;
        background: var(--gradient);
        padding: 24px 32px;
        border-radius: 16px;
      }

      .about-badge-value {
        font-size: 36px;
        font-weight: 800;
        color: white;
      }

      .about-badge-label {
        font-size: 13px;
        color: rgba(255,255,255,0.8);
      }

      .about-content .section-title {
        text-align: left;
        margin-bottom: 20px;
      }

      .about-desc {
        font-size: 17px;
        color: var(--text-muted);
        line-height: 1.8;
        margin-bottom: 32px;
      }

      .values-list {
        display: flex;
        flex-direction: column;
        gap: 12px;
      }

      .value-item {
        display: flex;
        align-items: center;
        gap: 16px;
        padding: 16px 20px;
        background: var(--bg-card);
        border: 1px solid var(--border);
        border-radius: 14px;
        transition: all 0.2s;
      }

      .value-item:hover {
        border-color: var(--border-hover);
      }

      .value-icon {
        width: 40px;
        height: 40px;
        background: var(--gradient);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .value-icon svg {
        width: 20px;
        height: 20px;
        stroke: white;
      }

      .value-text {
        font-size: 15px;
        font-weight: 600;
      }

      /* Contact */
      .contact {
        padding: 100px 80px;
        background: var(--bg-card);
        text-align: center;
        border-top: 1px solid var(--border);
        position: relative;
        overflow: hidden;
      }

      .contact::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 800px;
        height: 800px;
        background: radial-gradient(circle, rgba(147, 51, 234, 0.1) 0%, transparent 70%);
        pointer-events: none;
      }

      .contact-content {
        position: relative;
        z-index: 1;
      }

      .contact-title {
        font-size: clamp(36px, 5vw, 56px);
        font-weight: 800;
        margin-bottom: 20px;
        letter-spacing: -0.02em;
      }

      .contact-title .gradient {
        background: var(--gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .contact-desc {
        font-size: 18px;
        color: var(--text-muted);
        max-width: 500px;
        margin: 0 auto 40px;
      }

      /* Footer */
      footer {
        background: var(--bg-dark);
        padding: 48px 80px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 1px solid var(--border);
      }

      .footer-logo img {
        height: 36px;
        width: auto;
      }

      .footer-links {
        display: flex;
        gap: 32px;
      }

      .footer-links a {
        font-size: 14px;
        color: var(--text-muted);
        text-decoration: none;
        transition: color 0.2s;
      }

      .footer-links a:hover { color: var(--text); }

      .footer-copy {
        font-size: 13px;
        color: var(--text-dim);
      }

      /* SVG Gradient Definition */
      .svg-defs {
        position: absolute;
        width: 0;
        height: 0;
      }

      /* Responsive */
      @media (max-width: 1024px) {
        header { padding: 16px 24px; }
        nav { display: none; }
        .hero { padding: 120px 40px 60px; }
        .hero-visual { display: none; }
        .hero-content { max-width: 100%; }
        .hero-stats { gap: 32px; flex-wrap: wrap; }
        .solutions { padding: 60px 40px; }
        .solutions-grid { grid-template-columns: repeat(2, 1fr); }
        .products { padding: 60px 40px; }
        .products-grid { grid-template-columns: 1fr; }
        .about { grid-template-columns: 1fr; padding: 60px 40px; gap: 40px; }
        .about-image-wrap { order: -1; }
        .contact { padding: 60px 40px; }
        footer { flex-direction: column; gap: 24px; text-align: center; padding: 40px; }
      }

      @media (max-width: 640px) {
        .solutions-grid { grid-template-columns: 1fr; }
      }
    </style>
  </head>
  <body>
    <!-- SVG Gradient Definition for icons -->
    <svg class="svg-defs">
      <defs>
        <linearGradient id="icon-gradient" x1="0%" y1="0%" x2="100%" y2="100%">
          <stop offset="0%" style="stop-color:#E91E8C"/>
          <stop offset="50%" style="stop-color:#9333EA"/>
          <stop offset="100%" style="stop-color:#0EA5E9"/>
        </linearGradient>
      </defs>
    </svg>

    <div class="bg-orbs">
      <div class="orb orb-1"></div>
      <div class="orb orb-2"></div>
      <div class="orb orb-3"></div>
    </div>

    <div class="concept-nav">
      <a href="/" title="Original">⌂</a>
      <a href="/concept-1" title="Soft Gradient">1</a>
      <a href="/concept-2" class="active" title="Dark Tech">2</a>
      <a href="/concept-3" title="Natural Green">3</a>
      <a href="/concept-4" title="Bold Industrial">4</a>
      <a href="/concept-5" title="Minimal Editorial">5</a>
    </div>

    <header>
      <a href="/" class="logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <nav>
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
      <a href="https://store.elmlakelabs.com" class="header-cta" target="_blank">Shop Now</a>
    </header>

    <section class="hero">
      <div class="hero-content">
        <div class="hero-badge">
          <span class="dot"></span>
          Precision Agriculture Technology
        </div>
        <h1 class="hero-title">
          The Future of Farming is <span class="gradient">Precise</span>
        </h1>
        <p class="hero-desc">
          State-of-the-art GPS guidance, auto-steer systems, and farm management
          software that help you unlock your full potential.
        </p>
        <div class="hero-cta-group">
          <a href="#products" class="btn-primary">
            Explore Products
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a href="#about" class="btn-secondary">Learn More</a>
        </div>
        <div class="hero-stats">
          <div>
            <div class="hero-stat-value">100+</div>
            <div class="hero-stat-label">Farms Equipped</div>
          </div>
          <div>
            <div class="hero-stat-value">±1"</div>
            <div class="hero-stat-label">RTK Accuracy</div>
          </div>
          <div>
            <div class="hero-stat-value">24/7</div>
            <div class="hero-stat-label">Expert Support</div>
          </div>
        </div>
      </div>
      <div class="hero-visual">
        <img src="/images/hero-field.jpg" alt="Agricultural field" class="hero-image" />
        <div class="hero-card top">
          <div class="hero-card-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <circle cx="12" cy="12" r="3"/>
            </svg>
          </div>
          <div class="hero-card-value">±1"</div>
          <div class="hero-card-label">RTK Accuracy</div>
        </div>
        <div class="hero-card bottom">
          <div class="hero-card-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            </svg>
          </div>
          <div class="hero-card-value">100+</div>
          <div class="hero-card-label">Farms Equipped</div>
        </div>
      </div>
    </section>

    <section id="solutions" class="solutions">
      <div class="section-header">
        <span class="section-badge">Solutions</span>
        <h2 class="section-title">What We Offer</h2>
      </div>

      <div class="solutions-grid">
        <div class="solution-card">
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="var(--purple)" stroke-width="1.5">
              <circle cx="12" cy="12" r="10"/>
              <circle cx="12" cy="12" r="3"/>
              <path d="M12 2v4M12 18v4M2 12h4M18 12h4"/>
            </svg>
          </div>
          <h3 class="solution-title">GPS Guidance</h3>
          <p class="solution-desc">Sub-inch precision guidance that reduces overlap and enables 24/7 operation.</p>
          <a href="#products" class="solution-link">Learn more →</a>
        </div>
        <div class="solution-card">
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="var(--purple)" stroke-width="1.5">
              <rect x="3" y="3" width="18" height="18" rx="2"/>
              <path d="M3 9h18M9 9v12"/>
            </svg>
          </div>
          <h3 class="solution-title">Land Leveling</h3>
          <p class="solution-desc">3D GPS-based leveling for perfect field grades and water efficiency.</p>
          <a href="#products" class="solution-link">Learn more →</a>
        </div>
        <div class="solution-card">
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="var(--purple)" stroke-width="1.5">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <h3 class="solution-title">Expert Support</h3>
          <p class="solution-desc">Personalized guidance from specialists who understand your operation.</p>
          <a href="#contact" class="solution-link">Contact us →</a>
        </div>
        <div class="solution-card">
          <div class="solution-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="var(--purple)" stroke-width="1.5">
              <rect x="2" y="3" width="20" height="14" rx="2"/>
              <path d="M8 21h8M12 17v4"/>
            </svg>
          </div>
          <h3 class="solution-title">Farm Software</h3>
          <p class="solution-desc">Blossom: field records, tracking, and analytics in one platform.</p>
          <a href="#products" class="solution-link">Learn more →</a>
        </div>
      </div>
    </section>

    <section id="products" class="products">
      <div class="section-header">
        <span class="section-badge">Equipment</span>
        <h2 class="section-title">Featured Products</h2>
      </div>

      <div class="products-grid">
        <div class="product-card">
          <div class="product-image-wrap">
            <img src="/images/products/at2-autosteer.png" alt="FJD AT2" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Auto-Steer System</div>
            <h3 class="product-name">FJD AT2</h3>
            <p class="product-desc">Advanced auto-steering with centimeter-level accuracy. Universal mount.</p>
            <div class="product-footer">
              <span class="product-price">$7,199</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Shop →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrap">
            <img src="/images/products/al01-land-leveling.png" alt="FJD AL01" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Land Leveling</div>
            <h3 class="product-name">FJD AL01 3D</h3>
            <p class="product-desc">Precision 3D land leveling for perfect field grades.</p>
            <div class="product-footer">
              <span class="product-price">$6,799</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Shop →</a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrap">
            <img src="/images/products/trion-v1t-rover.png" alt="FJD Trion V1t" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">RTK Rover</div>
            <h3 class="product-name">FJD Trion V1t</h3>
            <p class="product-desc">Professional RTK rover for surveying and mapping.</p>
            <div class="product-footer">
              <span class="product-price">$4,249</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">Shop →</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="about" class="about">
      <div class="about-image-wrap">
        <img src="/images/hero-field.jpg" alt="Agricultural field" class="about-image" />
        <div class="about-badge">
          <div class="about-badge-value">100+</div>
          <div class="about-badge-label">Farms Equipped</div>
        </div>
      </div>
      <div class="about-content">
        <span class="section-badge">About Us</span>
        <h2 class="section-title">Transforming Agriculture Through <span class="gradient">Innovation</span></h2>
        <p class="about-desc">
          Elm Lake Labs is dedicated to helping farmers unlock their full potential
          through state-of-the-art precision technology. From small family farms to
          large commercial operations, we provide the tools and expertise you need.
        </p>
        <div class="values-list">
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </div>
            <span class="value-text">Innovation — Cutting-edge technology</span>
          </div>
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              </svg>
            </div>
            <span class="value-text">Support — Expert guidance 24/7</span>
          </div>
          <div class="value-item">
            <div class="value-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
              </svg>
            </div>
            <span class="value-text">Excellence — Exceeding expectations</span>
          </div>
        </div>
      </div>
    </section>

    <section id="contact" class="contact">
      <div class="contact-content">
        <h2 class="contact-title">Ready to Transform Your <span class="gradient">Operation?</span></h2>
        <p class="contact-desc">
          Let's discuss how precision agriculture technology can work for your farm.
        </p>
        <a href="mailto:hello@elmlakelabs.com" class="btn-primary">
          Get in Touch
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </section>

    <footer>
      <a href="/" class="footer-logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <div class="footer-links">
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a href="https://store.elmlakelabs.com" target="_blank">Shop</a>
      </div>
      <span class="footer-copy">© 2024 Elm Lake Labs</span>
    </footer>
  </body>
</html>
