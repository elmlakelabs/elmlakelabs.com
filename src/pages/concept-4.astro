---
/**
 * CONCEPT 4: "GRADIENT INDUSTRIAL"
 * Aesthetic: Modern industrial with tech gradient accents
 * Feel: Rugged, powerful, but polished - machinery meets innovation
 * Typography: Condensed industrial sans (Barlow Condensed) for impact
 * Colors: Dark slate/steel backgrounds with logo gradient as accent color
 * Unique element: Diagonal cuts, bold typography, gradient line accents
 */
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Elm Lake Labs | Concept 4 - Gradient Industrial</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@400;500;600;700;800;900&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
      :root {
        /* Logo-derived colors */
        --magenta: #E91E8C;
        --purple: #9333EA;
        --blue: #0EA5E9;
        --cyan: #06B6D4;

        /* Industrial palette */
        --black: #0A0A0F;
        --steel-dark: #12121A;
        --steel: #1A1A25;
        --steel-light: #252535;
        --gray-dark: #3A3A4A;
        --gray: #6B6B7B;
        --gray-light: #9A9AAA;
        --white: #FFFFFF;

        --font-display: 'Barlow Condensed', sans-serif;
        --font-body: 'Inter', sans-serif;
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }

      html { scroll-behavior: smooth; }

      body {
        font-family: var(--font-body);
        background: var(--black);
        color: var(--white);
        line-height: 1.6;
        overflow-x: hidden;
      }

      /* Grid pattern background */
      .grid-pattern {
        position: fixed;
        inset: 0;
        pointer-events: none;
        z-index: 0;
        opacity: 0.03;
        background-image:
          linear-gradient(90deg, var(--purple) 1px, transparent 1px),
          linear-gradient(var(--purple) 1px, transparent 1px);
        background-size: 80px 80px;
      }

      /* Concept switcher */
      .concept-nav {
        position: fixed;
        top: 20px;
        right: 20px;
        z-index: 1000;
        display: flex;
        gap: 4px;
        padding: 6px;
        background: var(--steel);
        border: 1px solid var(--gray-dark);
      }
      .concept-nav a {
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: var(--font-display);
        font-size: 14px;
        font-weight: 700;
        text-decoration: none;
        color: var(--gray);
        background: transparent;
        transition: all 0.2s;
      }
      .concept-nav a:hover {
        color: var(--white);
        background: var(--steel-light);
      }
      .concept-nav a.active {
        background: linear-gradient(135deg, var(--magenta), var(--purple));
        color: var(--white);
      }
      .concept-nav a.home {
        font-size: 16px;
      }

      /* Gradient border helper */
      .gradient-border {
        position: relative;
      }
      .gradient-border::before {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        height: 2px;
        background: linear-gradient(90deg, var(--magenta), var(--purple), var(--blue));
      }

      /* Header */
      header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        padding: 16px 60px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background: rgba(10, 10, 15, 0.95);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid var(--steel-light);
      }

      .logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .logo img {
        height: 44px;
        width: auto;
      }

      nav {
        display: flex;
        gap: 4px;
      }

      nav a {
        padding: 10px 20px;
        font-family: var(--font-display);
        font-size: 14px;
        font-weight: 600;
        color: var(--gray-light);
        text-decoration: none;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        background: var(--steel);
        transition: all 0.2s;
      }

      nav a:hover {
        background: var(--steel-light);
        color: var(--white);
      }

      .header-cta {
        padding: 14px 28px;
        background: linear-gradient(135deg, var(--magenta), var(--purple));
        color: white;
        font-family: var(--font-display);
        font-size: 14px;
        font-weight: 700;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        text-decoration: none;
        transition: all 0.2s;
        clip-path: polygon(8px 0, 100% 0, 100% calc(100% - 8px), calc(100% - 8px) 100%, 0 100%, 0 8px);
      }

      .header-cta:hover {
        filter: brightness(1.1);
        transform: translateY(-2px);
      }

      /* Hero */
      .hero {
        min-height: 100vh;
        display: flex;
        align-items: center;
        padding: 120px 80px 80px;
        position: relative;
      }

      .hero-bg-gradient {
        position: absolute;
        top: 0;
        right: 0;
        width: 60%;
        height: 100%;
        background: linear-gradient(135deg, rgba(233, 30, 140, 0.05), rgba(147, 51, 234, 0.08), rgba(14, 165, 233, 0.05));
        clip-path: polygon(20% 0, 100% 0, 100% 100%, 0 100%);
      }

      .hero-content {
        position: relative;
        z-index: 1;
        max-width: 700px;
      }

      .hero-label {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        padding: 8px 20px;
        background: var(--steel);
        border-left: 3px solid;
        border-image: linear-gradient(to bottom, var(--magenta), var(--purple)) 1;
        font-family: var(--font-body);
        font-size: 12px;
        color: var(--gray-light);
        text-transform: uppercase;
        letter-spacing: 0.15em;
        margin-bottom: 32px;
      }

      .hero-title {
        font-family: var(--font-display);
        font-size: clamp(64px, 10vw, 120px);
        font-weight: 900;
        line-height: 0.9;
        text-transform: uppercase;
        letter-spacing: -0.02em;
        margin-bottom: 32px;
      }

      .hero-title .line1 {
        display: block;
        color: var(--white);
      }

      .hero-title .line2 {
        display: block;
        background: linear-gradient(90deg, var(--magenta), var(--purple), var(--blue));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .hero-desc {
        max-width: 500px;
        font-size: 16px;
        color: var(--gray-light);
        line-height: 1.8;
        margin-bottom: 48px;
      }

      .hero-cta-group {
        display: flex;
        gap: 16px;
      }

      .btn-primary {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        padding: 20px 40px;
        background: linear-gradient(135deg, var(--magenta), var(--purple));
        color: white;
        font-family: var(--font-display);
        font-size: 16px;
        font-weight: 700;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        text-decoration: none;
        clip-path: polygon(10px 0, 100% 0, 100% calc(100% - 10px), calc(100% - 10px) 100%, 0 100%, 0 10px);
        transition: all 0.2s;
      }

      .btn-primary:hover {
        filter: brightness(1.1);
        transform: translateY(-2px);
      }

      .btn-secondary {
        display: inline-flex;
        align-items: center;
        gap: 12px;
        padding: 20px 40px;
        background: transparent;
        color: var(--white);
        font-family: var(--font-display);
        font-size: 16px;
        font-weight: 700;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        text-decoration: none;
        border: 2px solid var(--gray-dark);
        transition: all 0.2s;
      }

      .btn-secondary:hover {
        border-color: var(--purple);
        color: var(--purple);
      }

      /* Stats bar */
      .stats-bar {
        background: var(--steel);
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        position: relative;
      }

      .stats-bar::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, var(--magenta), var(--purple), var(--blue), var(--cyan));
      }

      .stat {
        padding: 48px 40px;
        text-align: center;
        border-right: 1px solid var(--gray-dark);
        position: relative;
      }

      .stat:last-child {
        border-right: none;
      }

      .stat-number {
        font-family: var(--font-display);
        font-size: 56px;
        font-weight: 900;
        background: linear-gradient(135deg, var(--white), var(--gray-light));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 1;
        margin-bottom: 8px;
      }

      .stat-label {
        font-size: 11px;
        color: var(--gray);
        text-transform: uppercase;
        letter-spacing: 0.2em;
      }

      /* Solutions */
      .solutions {
        padding: 100px 80px;
        position: relative;
      }

      .section-header {
        display: flex;
        align-items: flex-end;
        justify-content: space-between;
        margin-bottom: 60px;
        padding-bottom: 24px;
        border-bottom: 1px solid var(--steel-light);
      }

      .section-tag {
        font-size: 12px;
        background: linear-gradient(90deg, var(--magenta), var(--purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        font-weight: 600;
        margin-bottom: 8px;
      }

      .section-title {
        font-family: var(--font-display);
        font-size: clamp(40px, 6vw, 64px);
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 0.02em;
        line-height: 1;
      }

      .section-number {
        font-family: var(--font-display);
        font-size: 100px;
        font-weight: 900;
        background: linear-gradient(135deg, var(--steel-light), var(--steel));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 1;
      }

      .solutions-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 24px;
      }

      .solution-card {
        padding: 40px;
        background: var(--steel);
        position: relative;
        transition: all 0.3s ease;
        border-left: 3px solid var(--gray-dark);
      }

      .solution-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 0;
        height: 100%;
        background: linear-gradient(180deg, rgba(233, 30, 140, 0.05), rgba(147, 51, 234, 0.08));
        transition: width 0.4s;
      }

      .solution-card:hover {
        border-left-color: var(--magenta);
        transform: translateX(8px);
      }

      .solution-card:hover::before {
        width: 100%;
      }

      .solution-content {
        position: relative;
        z-index: 1;
      }

      .solution-number {
        font-family: var(--font-display);
        font-size: 72px;
        font-weight: 900;
        background: linear-gradient(135deg, var(--steel-light), var(--gray-dark));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 1;
        margin-bottom: 16px;
      }

      .solution-title {
        font-family: var(--font-display);
        font-size: 28px;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-bottom: 12px;
      }

      .solution-desc {
        font-size: 14px;
        color: var(--gray-light);
        line-height: 1.7;
        margin-bottom: 24px;
      }

      .solution-link {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        font-family: var(--font-display);
        font-size: 13px;
        font-weight: 700;
        background: linear-gradient(90deg, var(--magenta), var(--purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-decoration: none;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        transition: gap 0.2s;
      }

      .solution-link span {
        color: var(--magenta);
        -webkit-text-fill-color: var(--magenta);
      }

      .solution-link:hover {
        gap: 12px;
      }

      /* Products */
      .products {
        padding: 100px 80px;
        background: var(--steel-dark);
        position: relative;
      }

      .products::before {
        content: 'PRODUCTS';
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        font-family: var(--font-display);
        font-size: clamp(80px, 12vw, 160px);
        font-weight: 900;
        color: var(--steel);
        opacity: 0.5;
        pointer-events: none;
        white-space: nowrap;
        letter-spacing: 0.1em;
      }

      .products-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 24px;
        margin-top: 60px;
        position: relative;
        z-index: 1;
      }

      .product-card {
        background: var(--steel);
        border: 1px solid var(--gray-dark);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
      }

      .product-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, var(--magenta), var(--purple), var(--blue));
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.3s;
      }

      .product-card:hover::before {
        transform: scaleX(1);
      }

      .product-card:hover {
        border-color: var(--purple);
        transform: translateY(-8px);
      }

      .product-image-wrapper {
        background: var(--steel-light);
        padding: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 200px;
      }

      .product-image {
        width: 100%;
        height: 140px;
        object-fit: contain;
        transition: transform 0.3s;
      }

      .product-card:hover .product-image {
        transform: scale(1.05);
      }

      .product-content {
        padding: 28px;
      }

      .product-category {
        font-size: 10px;
        background: linear-gradient(90deg, var(--magenta), var(--purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        font-weight: 600;
        margin-bottom: 8px;
      }

      .product-name {
        font-family: var(--font-display);
        font-size: 24px;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-bottom: 8px;
      }

      .product-desc {
        font-size: 13px;
        color: var(--gray-light);
        line-height: 1.6;
        margin-bottom: 24px;
      }

      .product-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 20px;
        border-top: 1px solid var(--gray-dark);
      }

      .product-price {
        font-family: var(--font-display);
        font-size: 28px;
        font-weight: 900;
        background: linear-gradient(90deg, var(--magenta), var(--purple));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .product-link {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 10px 20px;
        background: var(--steel-light);
        color: var(--white);
        font-family: var(--font-display);
        font-size: 12px;
        font-weight: 700;
        text-decoration: none;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        transition: all 0.2s;
      }

      .product-link:hover {
        background: linear-gradient(135deg, var(--magenta), var(--purple));
      }

      /* About */
      .about {
        padding: 100px 80px;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 80px;
        align-items: center;
        position: relative;
      }

      .about-visual {
        position: relative;
      }

      .about-image {
        width: 100%;
        height: 450px;
        object-fit: cover;
        filter: contrast(1.05);
      }

      .about-image-frame {
        position: absolute;
        inset: -16px;
        border: 2px solid;
        border-image: linear-gradient(135deg, var(--magenta), var(--purple), var(--blue)) 1;
        pointer-events: none;
      }

      .about-content h2 {
        font-family: var(--font-display);
        font-size: clamp(36px, 5vw, 52px);
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 0.02em;
        margin-bottom: 24px;
        line-height: 1.1;
      }

      .about-content h2 span {
        background: linear-gradient(90deg, var(--magenta), var(--purple), var(--blue));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .about-content > p {
        font-size: 15px;
        color: var(--gray-light);
        line-height: 1.9;
        margin-bottom: 40px;
      }

      .specs-list {
        display: grid;
        gap: 12px;
      }

      .spec-item {
        display: flex;
        align-items: center;
        gap: 16px;
        padding: 20px 24px;
        background: var(--steel);
        border-left: 3px solid var(--gray-dark);
        transition: all 0.3s;
      }

      .spec-item:hover {
        border-left-color: var(--magenta);
        background: var(--steel-light);
      }

      .spec-icon {
        width: 48px;
        height: 48px;
        background: linear-gradient(135deg, var(--magenta), var(--purple));
        display: flex;
        align-items: center;
        justify-content: center;
        clip-path: polygon(6px 0, 100% 0, 100% calc(100% - 6px), calc(100% - 6px) 100%, 0 100%, 0 6px);
      }

      .spec-icon svg {
        width: 24px;
        height: 24px;
        stroke: var(--white);
      }

      .spec-text {
        font-family: var(--font-display);
        font-size: 16px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
      }

      /* Contact */
      .contact {
        padding: 120px 80px;
        background: var(--steel-dark);
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .contact::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, var(--magenta), var(--purple), var(--blue), var(--cyan));
      }

      .contact-bg-text {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        font-family: var(--font-display);
        font-size: clamp(100px, 15vw, 200px);
        font-weight: 900;
        color: var(--steel);
        opacity: 0.4;
        pointer-events: none;
        white-space: nowrap;
        letter-spacing: 0.1em;
      }

      .contact-content {
        position: relative;
        z-index: 1;
      }

      .contact-title {
        font-family: var(--font-display);
        font-size: clamp(48px, 7vw, 80px);
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 0.02em;
        margin-bottom: 24px;
        line-height: 1;
      }

      .contact-title span {
        background: linear-gradient(90deg, var(--magenta), var(--purple), var(--blue));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .contact-desc {
        font-size: 16px;
        color: var(--gray-light);
        max-width: 500px;
        margin: 0 auto 48px;
        line-height: 1.8;
      }

      /* Footer */
      footer {
        background: var(--black);
        padding: 48px 80px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-top: 1px solid var(--steel);
      }

      .footer-logo {
        display: flex;
        align-items: center;
        text-decoration: none;
      }

      .footer-logo img {
        height: 36px;
        width: auto;
      }

      .footer-links {
        display: flex;
        gap: 4px;
      }

      .footer-links a {
        padding: 10px 16px;
        font-family: var(--font-display);
        font-size: 12px;
        font-weight: 600;
        color: var(--gray);
        text-decoration: none;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        background: var(--steel);
        transition: all 0.2s;
      }

      .footer-links a:hover {
        background: linear-gradient(135deg, var(--magenta), var(--purple));
        color: var(--white);
      }

      .footer-copy {
        font-size: 11px;
        color: var(--gray);
        text-transform: uppercase;
        letter-spacing: 0.1em;
      }

      /* Responsive */
      @media (max-width: 1024px) {
        .hero { padding: 120px 40px 60px; }
        .hero-bg-gradient { display: none; }
        .stats-bar { grid-template-columns: repeat(2, 1fr); }
        .solutions { padding: 60px 40px; }
        .section-header { flex-direction: column; align-items: flex-start; gap: 16px; }
        .section-number { display: none; }
        .solutions-grid { grid-template-columns: 1fr; }
        .products { padding: 60px 40px; }
        .products-grid { grid-template-columns: 1fr; }
        .about { grid-template-columns: 1fr; padding: 60px 40px; }
        .about-visual { order: -1; }
        header { padding: 12px 24px; }
        nav { display: none; }
        .contact { padding: 80px 40px; }
        footer { flex-direction: column; gap: 24px; text-align: center; padding: 40px; }
        .footer-links { flex-wrap: wrap; justify-content: center; }
      }
    </style>
  </head>
  <body>
    <div class="grid-pattern"></div>

    <!-- Concept Switcher -->
    <div class="concept-nav">
      <a href="/" class="home" title="Original">&#8962;</a>
      <a href="/concept-1" title="Soft Gradient">1</a>
      <a href="/concept-2" title="Dark Tech">2</a>
      <a href="/concept-3" title="Aurora Flow">3</a>
      <a href="/concept-4" class="active" title="Gradient Industrial">4</a>
      <a href="/concept-5" title="Minimal Luxe">5</a>
    </div>

    <header>
      <a href="/" class="logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <nav>
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
      <a href="https://store.elmlakelabs.com" class="header-cta" target="_blank">Shop</a>
    </header>

    <section class="hero">
      <div class="hero-bg-gradient"></div>
      <div class="hero-content">
        <div class="hero-label">
          <span>Precision Agriculture Technology</span>
        </div>
        <h1 class="hero-title">
          <span class="line1">Built For</span>
          <span class="line2">The Field</span>
        </h1>
        <p class="hero-desc">
          Heavy-duty GPS guidance and auto-steer systems engineered for real-world
          farming conditions. Sub-inch accuracy. Zero compromise.
        </p>
        <div class="hero-cta-group">
          <a href="#products" class="btn-primary">
            View Equipment
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a href="#about" class="btn-secondary">Learn More</a>
        </div>
      </div>
    </section>

    <div class="stats-bar">
      <div class="stat">
        <div class="stat-number">100+</div>
        <div class="stat-label">Farms Equipped</div>
      </div>
      <div class="stat">
        <div class="stat-number">&#177;1"</div>
        <div class="stat-label">RTK Accuracy</div>
      </div>
      <div class="stat">
        <div class="stat-number">24/7</div>
        <div class="stat-label">Support Ready</div>
      </div>
      <div class="stat">
        <div class="stat-number">15%</div>
        <div class="stat-label">Fuel Reduction</div>
      </div>
    </div>

    <section id="solutions" class="solutions">
      <div class="section-header">
        <div>
          <div class="section-tag">Capabilities</div>
          <h2 class="section-title">Our Solutions</h2>
        </div>
        <div class="section-number">01</div>
      </div>

      <div class="solutions-grid">
        <div class="solution-card">
          <div class="solution-content">
            <div class="solution-number">01</div>
            <h3 class="solution-title">GPS Guidance</h3>
            <p class="solution-desc">
              Sub-inch precision guidance systems that reduce overlap, save fuel,
              and enable 24/7 operation in any conditions.
            </p>
            <a href="#products" class="solution-link">
              View Systems <span>&#8594;</span>
            </a>
          </div>
        </div>
        <div class="solution-card">
          <div class="solution-content">
            <div class="solution-number">02</div>
            <h3 class="solution-title">Land Leveling</h3>
            <p class="solution-desc">
              3D laser and GPS-based leveling for perfect field grades,
              improved drainage, and maximum water efficiency.
            </p>
            <a href="#products" class="solution-link">
              Explore <span>&#8594;</span>
            </a>
          </div>
        </div>
        <div class="solution-card">
          <div class="solution-content">
            <div class="solution-number">03</div>
            <h3 class="solution-title">Expert Support</h3>
            <p class="solution-desc">
              Personalized guidance from precision ag specialists who understand
              your operation and help maximize ROI.
            </p>
            <a href="#contact" class="solution-link">
              Contact Us <span>&#8594;</span>
            </a>
          </div>
        </div>
        <div class="solution-card">
          <div class="solution-content">
            <div class="solution-number">04</div>
            <h3 class="solution-title">Farm Software</h3>
            <p class="solution-desc">
              Blossom: comprehensive field records, nutrient tracking, pest management,
              and harvest analytics in one platform.
            </p>
            <a href="#products" class="solution-link">
              Learn More <span>&#8594;</span>
            </a>
          </div>
        </div>
      </div>
    </section>

    <section id="products" class="products">
      <div class="section-header">
        <div>
          <div class="section-tag">Hardware</div>
          <h2 class="section-title">Equipment</h2>
        </div>
      </div>

      <div class="products-grid">
        <div class="product-card">
          <div class="product-image-wrapper">
            <img src="/images/products/at2-autosteer.png" alt="FJD AT2" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Auto-Steer System</div>
            <h3 class="product-name">FJD AT2</h3>
            <p class="product-desc">Advanced auto-steering with centimeter-level accuracy. Universal mount.</p>
            <div class="product-footer">
              <span class="product-price">$7,199</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">
                Order &#8594;
              </a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrapper">
            <img src="/images/products/al01-land-leveling.png" alt="FJD AL01" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">Land Leveling</div>
            <h3 class="product-name">FJD AL01 3D</h3>
            <p class="product-desc">Precision 3D land leveling for perfect field grades.</p>
            <div class="product-footer">
              <span class="product-price">$6,799</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">
                Order &#8594;
              </a>
            </div>
          </div>
        </div>
        <div class="product-card">
          <div class="product-image-wrapper">
            <img src="/images/products/trion-v1t-rover.png" alt="FJD Trion V1t" class="product-image" />
          </div>
          <div class="product-content">
            <div class="product-category">RTK Rover</div>
            <h3 class="product-name">FJD Trion V1t</h3>
            <p class="product-desc">Professional RTK rover for surveying and mapping.</p>
            <div class="product-footer">
              <span class="product-price">$4,249</span>
              <a href="https://store.elmlakelabs.com" class="product-link" target="_blank">
                Order &#8594;
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section id="about" class="about">
      <div class="about-visual">
        <img src="/images/hero-field.jpg" alt="Agricultural field" class="about-image" />
        <div class="about-image-frame"></div>
      </div>
      <div class="about-content">
        <div class="section-tag">Company</div>
        <h2>Built to <span>Perform</span></h2>
        <p>
          Elm Lake Labs is dedicated to helping farmers unlock their full potential
          through state-of-the-art precision technology. From small family farms to
          large commercial operations, we provide the tools, expertise, and support.
        </p>
        <div class="specs-list">
          <div class="spec-item">
            <div class="spec-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </div>
            <span class="spec-text">Innovation &#8212; Cutting-edge technology</span>
          </div>
          <div class="spec-item">
            <div class="spec-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              </svg>
            </div>
            <span class="spec-text">Support &#8212; Expert guidance 24/7</span>
          </div>
          <div class="spec-item">
            <div class="spec-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke-width="2">
                <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
              </svg>
            </div>
            <span class="spec-text">Excellence &#8212; Exceeding expectations</span>
          </div>
        </div>
      </div>
    </section>

    <section id="contact" class="contact">
      <div class="contact-bg-text">CONTACT</div>
      <div class="contact-content">
        <h2 class="contact-title">Ready To <span>Upgrade?</span></h2>
        <p class="contact-desc">
          Let's discuss how precision agriculture technology can transform your operation.
        </p>
        <a href="mailto:hello@elmlakelabs.com" class="btn-primary">
          Get in Touch
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </section>

    <footer>
      <a href="/" class="footer-logo">
        <img src="/images/logo-white.png" alt="Elm Lake Labs" />
      </a>
      <div class="footer-links">
        <a href="#solutions">Solutions</a>
        <a href="#products">Products</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
        <a href="https://store.elmlakelabs.com" target="_blank">Shop</a>
      </div>
      <span class="footer-copy">&copy; 2024 Elm Lake Labs / All Rights Reserved</span>
    </footer>
  </body>
</html>
