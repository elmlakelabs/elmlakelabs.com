---
---

<section class="relative min-h-screen flex items-center pt-20 pb-16 px-4 md:px-8 overflow-hidden">
  <!-- Background image with overlay -->
  <div class="absolute inset-0">
    <img
      src="/images/hero-field.jpg"
      alt="Cranberry harvest"
      class="w-full h-full object-cover"
    />
    <div class="absolute inset-0 bg-gradient-to-r from-[var(--bg-primary)] via-[var(--bg-primary)]/90 to-[var(--bg-primary)]/60"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-[var(--bg-primary)] via-transparent to-[var(--bg-primary)]/80"></div>
  </div>

  <!-- Molecular pattern overlay -->
  <div class="absolute inset-0 molecular-bg opacity-50"></div>

  <!-- Animated gradient orbs -->
  <div class="absolute top-1/4 -left-32 w-96 h-96 bg-[var(--brand-magenta)] rounded-full blur-[150px] opacity-20 animate-float"></div>
  <div class="absolute bottom-1/4 right-0 w-80 h-80 bg-[var(--brand-purple)] rounded-full blur-[120px] opacity-15 animate-float" style="animation-delay: -3s;"></div>
  <div class="absolute top-1/2 right-1/4 w-64 h-64 bg-[var(--brand-blue)] rounded-full blur-[100px] opacity-10 animate-float" style="animation-delay: -1.5s;"></div>

  <div class="relative z-10 max-w-[1400px] mx-auto w-full">
    <div class="max-w-[680px]">
      <!-- Badge -->
      <div class="inline-flex items-center gap-3 px-4 py-2 bg-[var(--bg-card)]/80 backdrop-blur-sm border border-[var(--border-subtle)] rounded-full text-xs text-[var(--text-secondary)] mb-8 animate-fade-in-up">
        <span class="relative flex h-2.5 w-2.5">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-[var(--brand-magenta)] opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-[var(--brand-magenta)]"></span>
        </span>
        <span>Precision Agriculture Technology</span>
      </div>

      <!-- Headline -->
      <h1 class="heading-xl mb-6">
        <span class="block text-[var(--text-secondary)] animate-fade-in-up delay-100">The Future of</span>
        <span class="block animate-fade-in-up delay-200">Farming is</span>
        <span class="block animate-fade-in-up delay-300">
          <span class="gradient-text gradient-text-animated">Precise</span>
        </span>
      </h1>

      <!-- Subheadline -->
      <p class="max-w-[520px] text-lg md:text-xl text-[var(--text-secondary)] leading-relaxed mb-10 animate-fade-in-up delay-400">
        State-of-the-art GPS guidance, auto-steer systems, and farm management
        software that help you unlock your full potential.
      </p>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row gap-4 animate-fade-in-up delay-500">
        <a href="#products" class="btn btn-primary">
          Explore Products
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
        <a href="#about" class="btn btn-secondary">Learn More</a>
      </div>

      <!-- FJD Partner Badge -->
      <div class="flex flex-col gap-6 mt-12 pt-8 border-t border-[var(--border-subtle)] animate-fade-in-up delay-600">
        <div class="flex flex-col sm:flex-row items-start sm:items-center gap-6">
          <span class="text-xs text-[var(--brand-magenta)] uppercase tracking-[0.15em] font-semibold">Official Authorized Dealer</span>
          <div class="flex items-center justify-center px-6 py-4 bg-white rounded-xl shadow-lg">
            <img
              src="/images/fjd/fjd-logo.jpg"
              alt="FJDynamics"
              class="h-14 md:h-20 w-auto"
            />
          </div>
        </div>
        <div class="flex flex-wrap items-center gap-6 text-sm text-[var(--text-muted)]">
          <div class="flex items-center gap-2">
            <svg class="text-[var(--brand-purple)]" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <path d="M12 6v6l4 2"/>
            </svg>
            <span>24/7 Support</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="text-[var(--brand-blue)]" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
              <polyline points="22 4 12 14.01 9 11.01"/>
            </svg>
            <span>100+ Farms Equipped</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="hidden lg:flex absolute bottom-12 left-8 items-center gap-4 text-xs text-[var(--text-muted)] uppercase tracking-widest">
    <span>Scroll</span>
    <div class="w-16 h-px bg-[var(--border-medium)] relative overflow-hidden">
      <div class="absolute top-0 left-0 w-8 h-full bg-gradient-to-r from-[var(--brand-magenta)] to-[var(--brand-purple)]" style="animation: scrollLine 2s ease-in-out infinite;"></div>
    </div>
  </div>
</section>
